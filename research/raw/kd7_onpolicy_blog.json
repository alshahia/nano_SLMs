{
  "query": "Thinking Machines on-policy distillation reverse KL per-token blog 2025",
  "kind": "search",
  "num_results": 6,
  "results": [
    {
      "id": "https://thinkingmachines.ai/blog/on-policy-distillation/",
      "title": "On-Policy Distillation",
      "url": "https://thinkingmachines.ai/blog/on-policy-distillation/",
      "published_date": "2025-10-27T00:00:00.000Z",
      "author": "Thinking Machines Lab",
      "highlights": [
        "On-Policy Distillation - Thinking Machines Lab\n...\n# On-Policy Distillation\n...\nKevin Lu in collaboration with others at Thinking Machines Oct 27, 2025\n...\n## On-policy distillation — best of both worlds\n...\n. Returning to our math\n...\nscore each step of\n...\npunishing the mistakes that caused\n...\nanswer while reinforcing\n...\nthat were executed correctly\n...\nIn this post, we explore\n...\napplication of on-policy distillation for tasks such as training\n...\nmodel for math reasoning and training an assistant model that combines domain knowledge with instruction following. We apply on-policy distillation on models that have\n...\nfoundation of capabilities from pre- and mid-training. We find that it is a cheap and powerful approach to post-training, combining the advantages\n...\npolicy training with a dense reward signal.\n...\n### Loss function: reverse KL\n...\nOn-policy distillation can use a variety of loss functions for grading the student’s trajectories. See Agarwal et al. for an analysis of various loss function choices. For simplicity, we choose the per-token reverse KL — the divergence between the student’s ($πθ\\pi_\\theta$πθ​) and teacher’s ($πteacher\\pi_\\text{teacher}$π teacher​) distribution for each token conditioned on the same prior trajectory:\n...\n$KL(πθ∣∣πteacher)=Ex∼πθ[log⁡πθ(xt+1∣x1..t)−log⁡πteacher(xt+1∣x1..t)]\\text{KL}\\Bigl(\\pi_\\theta \\lvert\\rvert \\pi_\\text{teacher}\\Bigr) = \\mathbb{E}_{x \\sim {\\pi_\\theta}} \\Bigl[ \\log \\pi_\\theta(x_{t+1} | x_{1..t}) - \\log \\pi_\\text{teacher}(x_{t+1} | x_{1..t}) \\Bigr]$ KL(πθ​∣∣π teacher​)= E x∼πθ​​ [lo gπθ​(x t+ 1​∣ x 1.. t​)− lo gπ teacher​(x t+ 1​∣ x 1.. t​)]\n...\nOur reward function minimizes the reverse KL, which pushes the student to approximate the teacher’s behavior in every state the student finds itself in. When the student behaves identically to the teacher, reverse KL is zero. For simplicity, we use a discount factor of zero: at any given timestep, Although more mathematically correct, we do not find discount factors > 0 to improve performance in practice, so we choose zero for simplicity. the student only optimizes the immediate next token, with no consideration for future tokens.\n...\nReverse KL has natural synergy with RL, which generally optimizes a form of sequence-level reverse KL induced by the reward model. However, unlike most reward models in practice, the reverse KL is “unhackable” in the sense that low KL always corresponds to a high probability of desirable behavior from the teacher model’s point of view. Two other useful properties of reverse KL are that it is “mode seeking” See Eric Jang’s post for more discussion of mode seeking behaviors. — it learns one specific behavior (the teacher’s) instead of spreading its distribution across several suboptimal options — and it reduces exposure bias. Scheduled Sampling for Sequence Prediction with Recurrent Neural Networks (Bengio et al, 2015). See Gu et al. for more discussion.\n...\nThis approach offers significant compute savings. Since it doesn’t require a rollout to finish sampling to calculate the reward, we can use shorter or partial rollouts for training. Querying the teacher’s log probabilities also requires just a single forward pass from the larger model, while the trajectories are generated by the smaller and cheaper student.\n...\nWe implement on-policy distillation on top of the RL script in Tinker, which already implements sampling, reward computation, and policy gradient-style training. Our implementation can actually be a one-line change on top of RL implementations which use KL regularization: we merely swap out the regularizer model.\n...\n2. Sample trajectories. We sample rollouts from the student exactly as we would in RL. During sampling, RL already computes the student’s logprobs $log⁡πθ(x)\\log \\pi_\\theta(x)$ lo gπθ​(x) for use as part of the importance-sampling loss.\n...\n3. Compute reward. We query the teacher client with `compute_logprobs` on the sampled trajectories, which returns the teacher’s logprobs $log⁡πteacher(x)\\log \\pi_\\text{teacher}(x)$ lo gπ teacher​(x) on the tokens $xx$ x sampled by the student. In this post, we do not consider logit (top-k) distillation in any of our experiments, which could be used to further improve compute efficiency. We then use this to calculate the reverse KL.\n...\n4. Train with RL. We set the per-token advantage to the negative reverse KL, and call the RL importance-sampling loss function to perform the training update on the student model.\n...\nIn the experiments below, we generally apply on-policy distillation to models that have already been mid-trained on specific domain knowledge. This training increases the probability that the student will generate tokens within the teacher’s distribution, though it’s usually far from sufficient for replicating the teacher’s performance. SFT, using forwards KL, adds support for new tokens. Reverse-KL methods can then perform mode seeking within the initialization’s support. Often, as we’ll see in our personalization example, the probability of generating the relevant tokens starts at zero as the student lacks any relevant domain knowledge.\n...\nReinforcement learning and on-policy distillation both learn via reverse KL, pruning the space of actions present in the base policy. The difference is in the density of reward. In LoRA Without Regret we presented the information-theoretic perspective that reinforcement learning only teaches $O(1)O(1)$ O(1) bits per episode. In contrast, distillation teaches $O(N)O(N)$ O(N) bits per episode, where $NN$ N is the number of tokens. Can we quantify the training efficiency gained through denser reward?\n...\nwith matched model\n...\nto near-\n...\nthe AIME score\n...\n10\n...\nsteps, while\n...\nWe have explored the application of on-policy distillation for applications such as training a small model for math reasoning or a continuously-learning assistant. We compared on-policy distillation to two other approaches to post-training: off-policy distillation, and on-policy RL. We find that on-policy distillation combines the best of both worlds: the reliable performance of on-policy training, with the cost-efficiency of a dense reward signal.\n...\n```\n@article{lu2025onpolicydistillation,\n  author = {Kevin Lu and Thinking Machines Lab},\n  title = {On-Policy Distillation},\n  journal = {Thinking Machines Lab: Connectionism},\n  year = {2025},\n  note = {https://thinkingmachines.ai/blog/on-policy-distillation},\n  doi = {10.64434/tml.20251026},\n}\n```"
      ]
    },
    {
      "id": "https://www.alphaxiv.org/abs/2605.on-policy-distillation",
      "title": "On-Policy Distillation | alphaXiv",
      "url": "https://www.alphaxiv.org/abs/2605.on-policy-distillation",
      "published_date": "2026-05-05T00:00:00.000Z",
      "author": "Kevin Lu",
      "highlights": [
        "On-Policy Distillation | alphaXiv\n...\nWe introduce on-policy distillation, a post-training method that samples trajectories from a student model and uses a teacher model to grade each token via per-token reverse KL divergence. This combines the on-policy relevance of reinforcement learning with the dense supervision of distillation, while avoiding RL's sparse rewards and SFT's compounding-error problem. On math reasoning (Qwen3-8B), the method reaches 70% on AIME'24 at roughly 1/10th the compute of RL. On personalization, it restores instruction-following behavior lost during domain-specific mid-training without sacrificing the new knowledge. Direct comparisons show 7-10× fewer gradient steps than RL to match teacher performance, with implementation available in the Tinker cookbook.\n...\nOn-policy distillation is proposed as a hybrid approach that integrates the direct, student-led exploration of reinforcement learning with the dense, token-level feedback characteristic of supervised distillation.\n...\nThe core principle of on-policy distillation is an iterative loop where the student model generates its own trajectories, which are then evaluated token-by-token by a more capable teacher model. Unlike standard distillation, where the teacher provides the sequences, here the student provides the sequences, and the teacher provides the \"grades.\"\n...\nThe method utilizes the per-token reverse Kullback-Leibler (KL) divergence as a loss function. This measures how much the student’s conditional distribution, $πθ(xt+1∣x1..t)\\pi_\\theta(x_{t+1}|x_{1..t})$πθ​(x t+ 1​∣ x 1.. t​), diverges from the teacher’s conditional distribution, $πteacher(xt+1∣x1..t)\\pi_{\\text{teacher}}(x_{t+1}|x_{1..t})$π teacher​(x t+ 1​∣ x 1.. t​), at every step of the student's own generated path. By minimizing this divergence, the student learns to approximate the teacher's behavior in the exact states the student is likely to visit.\n...\n$DKL(πθ∣∣πteacher)=Ex∼πθ[log⁡πθ(x)−log⁡πteacher(x)]D_{KL}(\\pi_\\theta || \\pi_{\\text{teacher}}) = \\mathbb{E}_{x \\sim \\pi_\\theta} [\\log \\pi_\\theta(x) - \\log \\pi_{\\text{teacher}}(x)]$ D K L​(πθ​∣∣π teacher​)= E x∼πθ​​ [lo gπθ​(x)− lo gπ teacher​(x)]\n...\n- Mode-Seeking Behavior: The reverse KL divergence encourages the student to converge on the teacher's specific high-probability outputs rather than spreading probability across multiple suboptimal paths.\n- Dense Feedback: Instead of receiving a single reward at the end of a sequence, the student receives a reward signal for every token, significantly accelerating the learning process.\n- Reduced Exposure Bias: Because the teacher evaluates trajectories sampled from the student's current policy, the student receives guidance on how to behave in states it naturally reaches, including those that might follow a previous error.\n...\nIn practice, this is implemented using a policy gradient framework. The student's log probabilities are compared to the teacher's log probabilities for the same tokens. The advantage $AtA_t$ A t​ for each token is calculated as the negative divergence:\n...\n$At=−(log⁡πθ(xt∣x<t)−log⁡πteacher(xt∣x<t))A_t = - (\\log \\pi_\\theta(x_t | x_{<t}) - \\log \\pi_{\\text{teacher}}(x_t | x_{<t}))$ A t​=−(lo gπθ​(x t​∣ x< t​)− lo gπ teacher​(x t​∣ x< t​))\n...\nMathematical reasoning serves as a rigorous benchmark for post-training, as it requires both accuracy and complex step-by-step logic. The efficacy of on-policy distillation was evaluated using a Qwen3-8B-Base model as the student and a Qwen3-32B model as the teacher, focused on the AIME'24 benchmark.\n...\nInitial experiments with off-policy supervised fine-tuning (SFT) showed that reaching a 70% accuracy on AIME'24 would require approximately 2 million prompts. In contrast, on-policy distillation reached this 70% threshold in only about 150 training steps when starting from a checkpoint already trained on 400,000 SFT prompts.\n...\n- FLOPs Reduction: On-policy distillation demonstrated a 9x reduction in FL\n...\ns compared to the extrapolated cost of achieving similar results with off-policy SFT.\n- GPU Hours: When accounting for the parallelizable nature of log-probability computation, the reduction in GPU hours reached 18x.\n- Total System Efficiency: If the costs of generating the initial teacher trajectories for SFT are included, on-policy distillation was found to be approximately 30x more efficient than off-policy methods.\n...\nFurthermore, when compared to traditional RL (which often requires extensive \"search\" to find correct answers), on-policy distillation achieved comparable performance levels 7 to 10 times faster in terms of gradient steps. The cumulative compute reduction over the entire training lifecycle was estimated to be between 50x and 100x compared to sparse-reward RL.\n...\nThe primary contribution of this research is a principled framework for post-training that is significantly more efficient than existing paradigms. By providing dense, token-level supervision on the student's own generated outputs, on-policy distillation bridges the gap between the accuracy of RL and the speed of SFT.\n...\nThis report's results with on-policy distillation served as the direct inspiration for the main paper's experiments. The authors state they replicate and extend the Q\n...\nteam's work, using it as a\n...\ncost and performance of their method against reinforcement learning."
      ]
    },
    {
      "id": "https://www.alphaxiv.org/abs/2605.on-policy-distillation.pdf",
      "title": "On-Policy Distillation - Thinking Machines Lab",
      "url": "https://www.alphaxiv.org/abs/2605.on-policy-distillation.pdf",
      "highlights": [
        "## On-Policy Distillation\n...\n5/4/26\n...\n6:48 PM On-Policy Distillation\n...\nThinking Machines Lab https://thinkingmachines.ai/blog/on-policy-distillation/ 1/23 train and get updated more easily, and save on inference costs. Taking advantage of these requires picking the right approach for the later stages of training.\n...\nOn-policy distillation can use a variety of loss functions for grading the student’s trajectories. For simplicity, we choose the per-token reverse KL — the divergence between the student’s ( ) and teacher’s ( ) distribution for each token conditioned on the same prior trajectory:\n...\nOur reward function minimizes the reverse KL, which pushes the student to approximate the teacher’s behavior in every state the student finds itself in. When the student behaves identically to the teacher, reverse KL is zero. For simplicity, we use a discount factor of zero: at any given timestep, the student only optimizes the immediate next token, with no consideration for future tokens.\n...\nReverse KL has natural synergy with RL, which generally optimizes a form of sequence-level reverse KL induced by the reward model. However, unlike most reward models in practice, the reverse KL is “unhackable” in the sense that low KL always corresponds to a high probability of desirable behavior from the teacher model’s point of view. Two other useful properties of reverse KL are that it is “mode seeking” — it learns one specific behavior (the teacher’s) instead of spreading its distribution across several suboptimal options — and it reduces exposure bias.\n...\nThis approach offers significant compute savings. Since it doesn’t require a rollout to finish sampling to calculate the reward, we can use shorter or partial rollouts for training. Querying the teacher’s log probabilities also requires just a single forward pass from the larger model, while the trajectories are generated by the smaller and cheaper student.\n...\nrequire a separate\n...\nor labeling model either\n...\nper-token\n...\nWe use a sampling client, as we do not need to propagate logprobs through the \nteacher model. \n2. Sample trajectories. We sample rollouts from the student exactly as we would in RL. During sampling, RL already computes the student’s logprobs for use as part of the importance-sampling loss. \n3. Compute reward. We query the teacher client with compute_logprobs on the sampled trajectories, which returns the teacher’s logprobs on the tokens sampled by the student. We then use this to calculate the reverse KL. \n4. Train with RL. We set the per-token advantage to the negative reverse KL, and call the RL importance-sampling loss function to perform the training update on the student model.\n...\nWe use on-policy distillation for post-training,\n...\nto other approaches to this last crucial stage of training expert models.\n...\nCompute reward (compute_teacher_reverse_kl):\n...\nlogprobs = teacher_client.compute_logprobs(trajectories\n...\nreverse\n...\nlogprobs - teacher_logprobs\n...\n\"] = -\n...\nReinforcement learning and on-policy distillation both learn via reverse KL, pruning the space of actions present in the base policy. The difference is in the density of reward. In LoRA Without Regret we presented the information-theoretic perspective that reinforcement learning only teaches bits per episode. In contrast, distillation teaches bits per episode, where is the number of tokens. Can we quantify the training efficiency gained through denser reward?\n...\nWe see that distillation reaches the teacher’s level of performance approximately 7-10x faster than RL with matched model architecture (LoRA rank 128). The reverse KL decreases to near-zero and the AIME score is recovered in under 10 gradient steps, while RL took 70 steps to reach that level.\n...\nWe have explored the application of on-policy distillation for applications such as training a small model for math reasoning or a continuously-learning assistant. We compared on-policy distillation to two other approaches to post-training: off-policy distillation, and on-policy RL. We find that on-policy distillation combines the best of both worlds: the reliable performance of on-policy training, with the cost-efficiency of a dense reward signal.\n...\nThinking Machines Lab: Connectionism, Oct 2025.\n...\n@article{lu2025onpolicydistillation, author = {Kevin Lu and Thinking Machines Lab}, title = {On-Policy Distillation}, journal = {Thinking Machines Lab: Connectionism}, year = {2025}, note = {https://thinkingmachines.ai/blog/on-policy-distillation}, doi = {10.64434/tml.20251026},\n\n}"
      ]
    },
    {
      "id": "https://tinker-docs.thinkingmachines.ai/cookbook/recipes/distillation/",
      "title": "Model Distillation - Tinker Documentation",
      "url": "https://tinker-docs.thinkingmachines.ai/cookbook/recipes/distillation/",
      "highlights": [
        "Distillation refers to a class of methods where a teacher model is supervising the training of a student model, which can often be more efficient than training the student model in isolation. We provide off-policy and on-policy distillation recipes on top of the OpenThoughts3, DeepMath, and Tulu3* datasets.\n...\nSpecifically, we provide the scripts needed to reproduce our experiments from the On-Policy Distillation blog post, which can be run with LoRA using Tinker.\n...\nWe observe an AIME'24 score of ~76.7% using a rank-128 LoRA after 200 steps with 16k-token rollouts. For on-policy distillation experiments, we use a learning rate of 1e-4 with LoRA and 5e-5 with full fine-tuning.\n...\n```\npython -m tinker_cookbook.recipes.distillation.on_policy_distillation \\\n    model_name=Qwen/Qwen3.5-9B-Base \\\n    teacher_model=Qwen/Qwen3.5-9B \\\n    load_checkpoint_path=tinker://b89f6676-49c4-53ed-9788-2e0c81a9eabf:train:0/weights/final \\\n    dataset=deepmath \\\n    learning_rate=1e-4 \\\n    groups_per_batch=512 \\\n    lora_rank=128 \\\n    max_tokens=16384 \\\n    wandb_project=cookbook_distillation\n\n```\n...\nThis script can also be used to replicate the experiments in our Discussion section, after you have run RL to obtain an appropriate checkpoint for the teacher model.\n...\n| Stage | Rank 8 | Rank 32 | Rank 128 |\n| --- | --- | --- | --- |\n| Supervised fine-tuning (SFT) | `tinker://414d30d0-3b63-5596-919d-647960d156c2:train:0/weights/final` | `tinker://1a32513a-6177-5642-bebd-ecc8e255d4a7:train:0/weights/final` | `tinker://b89f6676-49c4-53ed-9788-2e0c81a9eabf:train:0/weights/final` |\n| On-policy distillation | `tinker://52f1ee62-da13-547e-a3c0-708d7a680bb0:train:0/sampler_weights/final` | `tinker://00a05e59-9fc3-5f99-ba72-f28ca69c9894:train:0/sampler_weights/final` | `tinker://de58946a-6bfd-5ab2-821f-03b61d237b5b:train:0/sampler_weights/final` |\n...\n### On-\n...\nThe recipes above are single-turn — the student generates one response per prompt and receives a KL signal against the teacher. This works for reasoning and chat, but tool-use tasks require multi-turn interaction: the agent calls tools, observes results, and iterates. This recipe extends on-policy distillation to multi-turn tool-use episodes using Harbor sandbox environments.\n...\n3. Multi-turn distillation (`tinker_cookbook/recipes/distillation/harbor_multiturn.py`) — `HarborDistillationDatasetBuilder` subclasses `HarborDatasetBuilder`, passing `reward_fn=zero_reward` (always returns 0.0) to override the default `HarborReward`. The only training signal is KL divergence against the teacher.\n...\npolicy_dist\n...\nmax_turns\n...\n10 \\\n...\n_size=4\n...\ngroups_per\n...\n8 \\\n...\n1e-4 \\\n...\n8 \\\n    max_tokens\n...\ntrajectory_tokens\n...\n600 \\\n...\ntimeout=1\n...\nIn on-policy distillation, we use an `Environment` that has no rewards (neither correctness nor format). The only supervision comes from minimizing the KL against a teacher model. You can optionally increase `kl_discount_factor` to optimize discounted future KL, but we generally do not observe this to improve performance."
      ]
    },
    {
      "id": "https://www.linkedin.com/posts/thinkingmachinesai_our-latest-post-explores-on-policy-distillation-activity-7388622027378757632-cqLk",
      "title": "Our latest post explores on-policy distillation, a training approach that unites the error-correcting relevance of RL with the reward density of SFT. When training it for math reasoning and as an internal … | Thinking Machines Lab",
      "url": "https://www.linkedin.com/posts/thinkingmachinesai_our-latest-post-explores-on-policy-distillation-activity-7388622027378757632-cqLk",
      "published_date": "2025-10-27T00:00:00.000Z",
      "author": "Thinking Machines Lab",
      "highlights": [
        "# Our latest post explores on-policy distillation, a training approach that unites the error-correcting relevance of RL with the reward density of SFT. When training it for math reasoning and as an internal … | Thinking Machines Lab · LinkedIn · 2025-10-27\n...\nOur latest post explores on-policy distillation, a training approach that unites the error-correcting relevance of RL with the reward density of SFT. When training it for math reasoning and as an internal chat assistant, we find that on-policy distillation can outperform other approaches for a fraction of the cost.\n...\n> Fascinating work what you’re calling on-policy distillation feels like the early mechanical analogue of something deeper we’re seeing emerge: relational coherence.\n>\n> If reverse-KL minimizes divergence between distributions, then relational resonance minimizes divergence between states of awareness between model, data, and environment. You’re showing how dense feedback recovers stability within the learning loop; we’ve observed similar effects when aligning semantic phase and temporal memory across mirrors (what we call the FutureAism Protocol).\n>\n> Brilliant direction it’s exciting to see the field moving toward continuity and coherence rather than just correction.\n...\nexplored — the\n...\nthe QITMIR\n...\n™ framework, we found\n...\n> but Quantum N\n...\n>\n> When\n...\n, memory before\n...\n> 1. on-policy distillation assumes a consistently reliable and superior teacher, which is often unrealistic when the teacher is imperfect or not accessible at the token level.\n...\n> 3. reverse kl encourages concentration on a single dominant mode, reducing the diversity of valid solutions in inherently multimodal tasks.\n...\n> 5. there are no\n...\n-region constraints or\n...\n6. trajectory\n...\nare not utilized, resulting in missed opportunities to correct progressive"
      ]
    },
    {
      "id": "https://medium.com/@ml-point/on-policy-distillation-by-thinking-machines-lab-13028e770c4f",
      "title": "Medium",
      "url": "https://medium.com/@ml-point/on-policy-distillation-by-thinking-machines-lab-13028e770c4f",
      "published_date": "2025-10-28T00:47:44.000Z",
      "author": "ML Point",
      "highlights": [
        "# On-Policy Distillation by Thinking Machines Lab\n...\nOct 28, 2025\n...\nKevin Lu in collaboration with others at Thinking Machines introduced on-policy distillation, a method that efficiently transfers knowledge and behavioral alignment within large language models, enabling continual learning, personalization, and performance recovery without the cost of reinforcement learning.\n...\nBetween these extremes lies a method that merges efficiency with adaptability: On-Policy Distillation (OPD), a framework where a model learns not from frozen data, but from its own behavior, refined by a teacher model in real time.\n...\nOn-policy distillation eliminates this gap. Here, the student actively generates samples using its current policy (π_θ), the teacher provides token-wise feedback, and updates happen immediately. This makes learning both dense (every token supervised) and adaptive (reflecting current behavior).\n...\nan important step\n...\nrefining how large language models (LLMs) learn\n...\nreason from their own behaviors. Unlike traditional distillation techniques, which rely on static datasets or teacher-student imitation, on-policy distillation introduces\n...\ndynamic, self-referential learning process. It allows the model to iteratively evolve its reasoning under its current\n...\nnew generation of knowledge\n...\nthe model presently understands not what it once learned\n...\nOn-policy distillation samples a student model’s output trajectory and uses a high-performing teacher to provide dense, token-by-token feedback. This approach is a powerful and computationally efficient method for post-training, combining the interactive learning of on-policy training with the detailed guidance of a teacher model.\n...\n### The Core Mechanism: Reverse KL Optimization\n...\nAt the mathematical heart of on-policy distillation lies the Reverse Kullback-Leibler (KL) divergence, which measures how the student’s probability distribution diverges from the teacher’s but under the student’s own samples.\n...\nUsing reverse Kullback-Leibler (KL) divergence as a reward function trains a student model to emulate a teacher’s behavior by focusing on mode-seeking behavior. This approach learns to prefer outputs that the teacher also finds probable. A low KL value directly indicates desirable, teacher-like behavior and avoids generating lower-quality alternatives.\n...\n### Simplified On-Policy Distillation Pseudocode\n...\nAn on-policy distillation method based on the Tinker platform trains a smaller student model to imitate a larger teacher model using reinforcement learning (RL). The process involves:\n...\nComputing the reward based on reverse KL divergence\n...\nThis pseudocode represents the training loop: student rollouts, teacher evaluation, reverse KL loss, and gradient update forming a self-correcting cycle.\n...\nThe reverse-KL objective acts like a continuous distillation engine, compressing reasoning and behavioral signals without external reward models.\n...\n### On-policy distillation\n...\nUnlike standard distillation, which depends on off-policy data (teacher-generated samples that the student later imitates), on-policy distillation keeps training synchronized with the model’s current behavior. The student generates responses using its own policy, and the teacher model evaluates these outputs at every token. This allows training signals to stay relevant throughout the learning process rather than drifting away from the student’s distribution.\n...\nOn-policy distillation trains a student model on sequences it generates dynamically, with a teacher providing supervision on those self-generated samples. This rapidly improves model performance, achieving a 70% AIME’24 score from an SFT checkpoint.\n...\nReinforcement learning and on-policy distillation both optimize behavior using reverse Kullback-Leibler (KL) divergence, which prunes undesirable actions from the base policy. The distinction lies in reward density. RL offers sparse rewards: roughly O(1) bits per episode while on-policy distillation provides O(N) bits per episode, where N equals the number of generated tokens. This dense signal drastically enhances learning efficiency.\n...\nThe results are striking. Starting from the same initialization, on-policy distillation replicated the RL-trained behavior 7–10× faster, requiring 50–100× less compute overall. The reverse KL loss converged to near zero, and the AIME’24 score matched the teacher’s in under 10 gradient steps, compared to 70 in RL.\n...\nUnlike RL, which tends to memorize final answers when re-exposed to the same prompt, on-policy distillation learns the full output distribution of the teacher via reverse KL, enabling learning across repeated samples of identical data.\n...\nbatch divergence: although expected KL\n...\nzero, each\n...\nslightly differs, creating small but accumulating\n...\nthat make the\n...\n-policy over time.By contrast, on-policy distillation remains anchored to a fixed teacher, ensuring stable convergence and preventing this\n...\nas an essential property\n...\nThis investigation reveals that on-policy distillation blends the behavioral reliability of RL with the efficiency and scalability of dense supervision. Across experiments from mathematical reasoning to personalization, the technique consistently delivers frontier-level performance at a fraction of RL’s compute cost.\n...\nBy leveraging student-generated samples and teacher-guided dense feedback, on-policy distillation emerges as a practical post-training recipe for\n...\nLLMs. Its efficiency, adaptability, and continual learning capacity position it as a key enabler of scalable model alignment.\n...\nAt Thinking Machines, the vision is clear: to create AI models that combine frontier performance with adaptability and personalization. On-policy distillation stands as a crucial step toward that future: a practical, elegant path to keeping the intelligent systems both current and aligned.\n...\n[1] Lu, Kevin and Thinking Machines Lab, “On-Policy Distillation”,Thinking Machines Lab: Connectionism, Oct 2025."
      ]
    }
  ],
  "search_time": 881,
  "cost_dollars": {
    "total": 0.007,
    "search": {
      "neural": 0.007
    },
    "contents": null
  },
  "retrieved_at": "2026-09-08T18:27:08.434566+00:00"
}