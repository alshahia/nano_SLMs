# TASKS.md — live task list

One row per task; the table is the current truth, rewritten in place as
states change. Statuses: `pending` → `in_progress` → `done`, or
`blocked` (always say what gates it). User-gated tasks do not start
without the user's explicit go. Evidence links make every status auditable.

Seeded from HANDOFF §8 (2026-09-06); TASKS.md owns live status from now on.

## Now

| # | Task | Status | Gate / next action | Evidence |
|---|---|---|---|---|
| 1 | M3 target run (226.5M, ctx 1024, 5000 steps) — monitor to completion | `done` | **COMPLETED 2026-09-07 ~08:16 UTC on TU09FBO**: 5000/5000, zero-flag auto-resume from checkpoint-4500 (restored from checkpoint_backup\checkpoint-4500.zip at 4501/5000); train_summary.json: best 1.8512 @4000, final 1.8641 @5000, env fingerprint git c16179c / torch 2.14.0+cu126. History of interruptions (each handled by re-running the exact train command — auto-resume, zero flags): fresh relaunch 2026-09-06 10:56 on MUO4QK5; sustained ~6.5–7.9 s/it → ETA ~8–9 h | commits 5ab9096 (step-500 PASSED, eval 2.8567) + step-1000 milestone (eval 2.265 vs TU09FBO partial's 2.2755 — determinism check; ckpt-1000 2.54 GB on disk; eval ctx1024/batch2 no OOM); 2026-09-06 ~13:07 probe: step 1073, window 9.5-9.9 s/it → ETA ~10-11 h (thermal swing, normal); 2026-09-06 ~14:00 probe: step 1516, thermal swing ~13.2 s/it (normal), ETA ~20:00-21:30, completion watcher armed (background job; also flags a dead-training-process state); 2026-09-06 ~15:05 probe: step 2018, eval curve 2.8567@500 → 2.265@1000 → 2.0903@1500 → **1.9972@2000**; rotation VERIFIED (ckpt-500 dropped at save-2000; 1000/1500/2000 on disk; full 2.5 GB each); eval ctx1024/batch2 no OOM at every 500-step eval; **2026-09-06 16:22: user STOPPED the run at step 2292** — saved state = checkpoint-2000 (verified: global_step 2000, best 1.9972; ckpt-1000/1500/2000 on disk, ~2.5 GB each), ~292 steps since the last save lost (transformers saves on the 500-step cadence only — no save-on-interrupt); GPU freed, log frozen; RESUME = exact train command, zero flags, continues at 2001/5000; machine-move bundle BUILT 2026-09-06 ~16:45: `E:\python projects\nano_SLMs_m3_handoff\checkpoint-2000.zip` (2.72 GB, CRC-verified, sha256 7124b1…aa2d9e) + RESUME_ON_NEW_MACHINE.txt via scripts/bundle_checkpoint.py; `train_target.log`; `runs/target/logs/`; 2026-09-06 18:36: bundle ARRIVED on TU09FBO — resumed 2001/5000 (checkpoint-2000 extracted from the checkpoint_backup zip; best-path patched; cadence save+eval 100 / limit 3; transformers args-mismatch warning at resume is expected); 2026-09-06 19:07: nvlddmkm 153 GPU-driver fault killed the run pre-2100-save (not sleep/OOM/reboot); CUDA sanity PASS; RELAUNCHED ~19:11 from checkpoint-2000, verified at 2001/5000); 2026-09-07 07:14: user STOPPED at step 4593/5000 — last complete = checkpoint-4500 (11 files; eval 1.8610@4500, best 1.8512@4000), steps 4501–4593 discarded, zipped to checkpoint_backup\checkpoint-4500.zip (2,368 MB, 12 entries, tar exit 0); GPU freed (103 MiB); RESUME = exact train command → 4501/5000 |
| 2 | Disk-headroom watch during M3 | `in_progress` | 2026-09-06 probe: 10.13 GB free with ckpt-500+1000 both on disk (~5.1 GB); rotation (limit 3) caps steady state ~7.6 GB → comfortable; report before deleting anything; nothing without user approval. 2026-09-06 ~13:12 probe: 16.3 GB free (rose ~9 GB since the ~13:07 probe — not agent-initiated, likely user cleanup; re-measure at M3 completion) 2026-09-06 ~15:05 probe: **34.73 GB free** post-save-2000 (rose ~21 GB since the 13:12 probe — not trainer-initiated; all 3 checkpoints intact) 2026-09-06 18:36 probe: 32.2 GB free (run live on TU09FBO; 3 checkpoints + 2 zips on disk); 2026-09-07 07:14 post-stop probe: 33.3 GB free (dirs ckpt-3500/4000/4500 + checkpoint_backup zips 2000/4500) | free-disk probes this session; HANDOFF §3b; **2026-09-08 user-approved cleanup: 30.8 GB freed (12.66 → 43.46)** — target ckpt-3500/4500 (ckpt-4000 KEPT as the apex resume point per user decision), sft_v2 e2 ckpts+final (overfit evidence = committed reports), sft_v2_pilot + sft_u10_pilot + kd-s mid-run ckpts, Milestone B arm dirs (summaries committed), checkpoint_backup 4500.zip + sft_t1-resume.zip; 23 uncommitted trainer_states/eval_reports snapshotted first to checkpoint_backup\evidence_snapshot_2026-09-08; all keepers verified present (target/final, sft_v2_e1/final, pilot/final, kd-s finals, sft_t1-final.zip). NOTE: data/teacher/ (Qwen3.5-0.8B) NOT on disk — re-download via scripts/download_teacher.py (~3 min) before any Track D judge work; **evidence archived+committed under runs/evidence/2026-09-08_cleanup/** (60 files / 15 MB: B-arm + e2 tracked smalls restored from HEAD + the 23 pre-delete snapshot trainer_states/eval_reports). 2026-09-09 checkpoint_backup triage (user-approved): extracted checkpoint-sft_v2_e1-final.zip (sha256-verified vs H2_RESUME §8) into runs/sft_v2_e1 → final/ now has model.safetensors 864 MB (gitignored, weights-local policy); zip KEPT as the sole backup copy of the deliverable weights; REMOVED duplicates checkpoint-sft_t1-final.zip (runs/sft_t1/final weights verified on disk) + checkpoint-4500.zip (runs/target/checkpoint-4500 verified on disk, run done; ckpt-4000 apex never came to this machine — only on the other one) + stale RESUME_ON_NEW_MACHINE.txt (its checkpoint-2000.zip long gone); disk 7.3 → 12.97 GB free; sft_t1-resume.zip + evidence_snapshot_2026-09-08 were already absent (9/8 cleanup on the other machine); C12_T1_RESUME.txt kept but now partially stale (references the removed sft_t1 zips). 2026-09-09 runs/ cleanup (user-approved, recommended set, GPU verified idle): removed 11 mid-run checkpoint dirs — sft_t1 ckpt-1750/2000/2048 (resume abandoned when sft_t1-resume.zip was dropped 9/8), sft_t1_pilot ckpt-250/313, target ckpt-2000/5000 (run done; ckpt-4500 KEPT as this machine's M3 resume point — ckpt-4000 apex never came here), smoke ckpt-100/150/200 (gitignore 'disposable'), pilot ckpt-500; 11 trainer_state.json snapshotted first to checkpoint_backup\trainer_states_2026-09-09\; all finals verified present post-delete (target/sft_t1/sft_t1_pilot/sft_v2_e1/pilot/smoke); e2etest + pilot/checkpoint-1000 (contains optimizer.pt only — M2 handoff point) + sft_t1_pilot/final kept per user scope choice; disk 12.97 → 32.55 GB free; git tracked-diff unchanged (every removed path was gitignored). 2026-09-09 (user-approved): runs/e2etest (23 files, 195 MB: 2x ~90 MB smoke-arch weights + checkpoint-100) + data/e2etest (6 files, 4.9 MB tokens) DELETED - the U10/U11 e2e drill scratch, regenerable by re-running the e2e drill; small evidential JSONs snapshotted first to runs/evidence/2026-09-09_e2etest_cleanup/ (committed); disk 41.08 -> 41.28 GB free. 2026-09-09 (user request): RESUME PACK BUILT - checkpoint_backup/resume_pack_2026-09-09.zip = 6,227,497,311 B (5.8 GiB), sha256 ceda94adbce10e7af5bcd85a7b58fbbe550072bd1f02bf0c91d68437f705bd4e, 80 entries, verify OK (all CRC pass); fresh clone + this one zip = fully working setup (arcnames repo-relative, extract AT repo root; in-zip RESUME.md + BUNDLE_MANIFEST.json with git_commit aad7f94); contents = ALL 10 weights-bearing finals (target, sft_v2_e1, sft_t1, h2p2_mixed_lora, h2_copy_lora, pilot, kd-t2p-kd, kd-t2p-baseline, kd-t2p-kd-skew, smoke); excluded-by-design: 3 pilot finals (superseded), 4 de-weighted finals (kd-s x2 reproducible ~35 min), mid-run checkpoints of completed runs (target/checkpoint-4500 + pilot/checkpoint-1000 remain on this disk only), data/teacher (re-download ~3 min), .env (secrets); builder = scripts/bundle_resume_pack.py (new, committed); E: 35.48 GB free after. 2026-09-09 evening (rows-2/13/16 session): Track C arm rotated ckpts ALREADY gone (2026-09-09 triage) - only ~4 MB of configs/tokenizer/summaries remain per kd-t2p-* final; the arm weights survive ONLY inside resume_pack_2026-09-09.zip (all 3 model.safetensors entries verified present). USER-APPROVED deletion: checkpoint-sft_v2_e1-final.zip 868 MB, pre-delete CRC verify pack-entry == disk file (906,121,272 B, CRC 0xb9dcefc1) -> E: 35.48 -> 36.35 GB; milestone-D measurement -50 MB -> 36.30 GB. runs/target/checkpoint-4000 (2.6 GB) untouched (user keep-decision 2026-09-08) |
| 3 | Post-M3 exit: `eval.py` on final + commit metrics + HANDOFF final row | `done` | #1 done. eval.py DONE 2026-09-07: runs/target/final/eval_report.json val_loss 1.8641, ppl 6.45; HANDOFF §2 + §3b final rows written; metrics commit `ba98cb1` (13 files: final-dir small files + train_summary + eval_report + tfevents + HANDOFF/TASKS; weights stay LOCAL per LFS decision — no LFS objects needed, tokenizer.json 3.5 MB plain git). Push pending user go | HANDOFF §8.2, §7; runs/target/final/eval_report.json |
| 4 | C12 Tier 1 — Evol-Instruct trace SFT of the M3 model | `done` | User decision 2026-09-06: run ONLY from runs/target/final (SFT-from-checkpoint-1000 declined — 20%-trained base; runbook decision log). **PILOT DONE 2026-09-07**: (1) c12_preflight --pilot PASS 8/8 GATE READY (GPU 7.5/8 free post-M3); (2) pilot sft.py --pilot PASSED — 5000 pairs, 1 epoch, 313 steps @ ~3 s/it (~17.5 min), train_loss 1.26, eval_loss 1.102→1.095, isolated in runs/sft_t1_pilot (final/ written, train_summary.json: base runs/target/final, params 226.5M); (3) eval.py on runs/sft_t1_pilot/final: forgetting guard CSN val_loss 1.9149 vs 1.8641 pre-SFT = +2.7% (gate ≤ +10-15% → PASS); instruction eval 50 held-out: greedy ast pass 0.60, sampled 0.78 (temp 0.8); eval_report.json in final/. **FULL RUN DONE 2026-09-07 (user go)**: full-mode preflight PASS 8/8 GATE READY (23.1 GB free, runs/sft_t1 clean) → sft.py --config configs/sft_t1.yaml: 16376 pairs x 2 epochs, 2048 steps @ ~2.6-3.3 s/it (~1h56m, zero interruptions), train_loss 0.868, eval_loss 0.836 (best = final @2048); runs/sft_t1/final written (model.safetensors 906 MB stays LOCAL per gitignore/LFS decision); eval.py on runs/sft_t1/final: instruction ast pass-rate greedy 0.86 / sampled 0.88 (pilot 0.60/0.78 — big gain); forgetting guard CSN val_loss 2.0995 vs 1.8641 pre-SFT = **+12.6% — within the ≤ +10-15% gate but near the upper edge** (honest note: capability-forgetting tradeoff favors instruction gain 0.60→0.86); eval_report.json in final/. Metrics commit `cd7b52d` PUSHED to origin/main (19 files: pilot+full small final-dir + tfevents + TASKS); prior `ba98cb1` (M3 metrics) also PUSHED. **Portability bundle BUILT 2026-09-07**: `checkpoint_backup/checkpoint-sft_t1-final.zip` (909.76 MB STORED zip64, 8 files at arcname `runs/sft_t1/final/...`, sha256 `2266c969…24a4f3`, verify OK) for inference + `checkpoint_backup/checkpoint-sft_t1-resume.zip` (2.72 GB STORED zip64, 11 files at arcname `checkpoint-2048/...`, sha256 `572cbba1…1f07e6`, verify OK — carries optimizer.pt 1.8 GB + scheduler.pt + scaler.pt + rng_state.pth for zero-flag SFT resume from step 2049). `checkpoint_backup/C12_T1_RESUME.txt` covers both zips: inference quickstart, SFT resume procedure (zero flags per PLAN §5.3 — identical to train.py), Tier 2/3 continuity (re-download teacher via scripts/download_teacher.py, ~3 min), full venv recipe, expected results, gotchas. NO-NEEDED-FILES CHECKLIST inside txt covers everything tracked-in-git vs needs-re-fetching. Next tier per plan: rows 5/7 (Tier 2/3, user-gated) | HANDOFF §8.4; research/c12_runbook.md; scripts/c12_preflight.py; research/c12_distillation_plan.md; checkpoint_backup/checkpoint-sft_t1-{final,resume}.zip |
| 5 | C12 Tier 2 (on-policy alignment) / Tier 3 (intra-ladder KD) | `pending` | User-gated (not yet approved). Teacher PICKED by user 2026-09-06: Qwen/Qwen3.5-0.8B - VERIFIED pre-download (ungated, ~1.8 GB, Apache-2.0, 24 text layers h1024, vocab 248320, ctx 262k, chat template; transformers 5.16.1 loads Qwen3_5ForConditionalGeneration natively; multimodal arch - judge uses the text tower only); DOWNLOADED to gitignored data/teacher/ (scripts/download_teacher.py, job exit 0, ~3 min - network far faster than the 230 KB/s estimate; local AutoConfig load OK). Exa context search UNBLOCKED 2026-09-06 (user added .env keys; payloads in research/raw/) | research/c12_tier2_brief.md; HF config probe 2026-09-06; research/qwen35_teacher_tasks.json |
| 6 | Upgrade path: pilot data quality (ungated raw code / the-stack-v2 with token); Flash-Next/GDN hybrid architecture | `pending` | User-gated; PLAN A7 = plain GQA first, hybrid is post-M3 only | HANDOFF §8.3; research/qwen3_8_flash_next_research.md |
| 7 | C12 Tier 2 readiness brief (milestone: user picked option 1 of the 2026-09-06 window menu) | `in_progress` | Design decision doc + VERIFIED bitsandbytes sm_75 SUCCESS + teacher costs + the tokenizer-mismatch finding (logit KL undefined cross-tokenizer -> V1 judge-rerank recommended). The Tier 2 RUN itself stays gated on #5/#1 + Tier 1 pass + explicit download/GPU approvals | research/c12_tier2_brief.md; c12_distillation_report.md §6 |
| 8 | scripts/status.py phase dashboard (milestone: user menu option 2) | `done` | Built + tested 2026-09-06: GPU/disk line + per-phase latest checkpoint, eval curves (trainer_state + TensorBoard), final summaries, eval reports incl. ast pass-rates; read-only, safe beside a live run; also surfaced a stray runs/pilot/checkpoint-1000/optimizer.pt (~0.8 GB, user-cleanup candidate) | scripts/status.py; README status line |
| 9 | LoRA/PEFT efficient fine-tune option (milestone: user menu option 3) | `done` | 2026-09-06 user OK'd install+design: peft 0.20.0 installed via uv (dry-run first: 12 new pkgs, ZERO upgrades - safe beside live M3), import verified (transformers 5.16.1 / torch 2.14.0+cu126), CPU LoRA smoke PASS (r=8: 3072/19648 trainable, exact math); design+cost doc (ladder = Llama arch, standard LoRA target names; T r=16 = 4,849,664 trainable = 2.14pct, est ~2.0-2.5 GB vs 4.24 measured full); config template | research/lora_peft_design.md; configs/lora_example.yaml |
| 10 | Implement the peft hook in train.py/sft.py (LoRA option) | `done` | **DONE 2026-09-07, all 4 acceptance criteria PASS.** (1) sanity_check PASS on configs/lora_example.yaml (peft block uncommented; trainable 2,605,056 = EXACT design number, 2.52%); (2) CPU e2e kill/resume drill PASS (checkpoint saves ADAPTER-ONLY (adapter_model.safetensors), resume continues, final = merged FULL model 8.3 MB via merge_and_unload in save_final); (3) vram_probe --lora MEASURED on GPU: 0.72 GB peak allocated vs 1.91 GB same-arch full FT = -62% VRAM at 2.5% trainable (1,693 tok/s vs 3,396 = -50% pace, expected for frozen base); (4) merged final loads in eval/infer unchanged (full safetensors + config). Gotchas fixed along the way: peft was missing on TU09FBO venv (installed, 1 pkg 0 upgrades); CUDA_VISIBLE_DEVICES empty-string does NOT hide GPU on Windows (MEMORY 12); kill-mid-checkpoint-write crash fixed via trainer_state.json completeness guard in find_latest_checkpoint (train.py+sft.py+kd.py, MEMORY 13) | research/lora_peft_design.md; src/model.py; MEMORY lessons 12-13 |
| 11 | Milestone B - Throughput: 8-bit Adam + batch/accum retune + env fingerprint | `done` | DONE 2026-09-07 (gates measured per protocol doc). Arms @500 steps, same data/seed: A fp32 eval **2.5644** / 2,801 tok/s / probe peak 1.91 GB; B 8-bit b1/a32 eval **2.5603** (delta -0.0041, parity gate +/-0.02 PASS) / 2,854 tok/s (1.02x pace gate PASS) / probe peak **1.36 GB** (-563 MiB >= 500 MiB VRAM gate PASS) / kill+resume drill PASS (auto-resumed ck251, no loss bump); C 8-bit b2/a16 eval 2.5604 (parity) but 1,680 tok/s = 0.60x pace FAIL -> REJECTED (batch 2 buys nothing on the 6 GB card). **DECISION: adamw_bnb_8bit + batch 1/accum 32 is the recorded default for the NEXT pretrain**; fp32 kept as fallback. LoRA hook (row 10) GPU-probed: 2.61M trainable, peak **0.72 GB**, 1,799 tok/s | research/milestone_b_8bit_ab.md; runs/pilot-{ab-base,b8,b8-r2x16}/final/train_summary.json; TASKS row 18 |
| 12 | Milestone C - Evaluation upgrade: execution-based mini-eval + tokens/sec | `done` | DONE 2026-09-06 (user GO window): scripts/mini_eval.py - 16 tasks x 2 tests, per-test subprocess+timeout (10 s, fresh temp workdir), canned self-test pass@1 1.0000 16/16 (harness mechanics proven), real CPU runs on runs/smoke/final x2 IDENTICAL (pass@1 0.0000 / test_credit 0.0000 both - greedy deterministic; 0.0 expected for a raw 200-step base model, harness measures post-SFT deltas); scripts/status.py tokens/sec + est-MFU lines live-tested read-only (target recent ~3,033 tok/s = ~10.8 s/it consistent with observed pace; MFU 20.4% fp32 / 4.4% fp16-tensor, est N=226.5M matches; pilot 2,021 tok/s full + MFU 17.2%/3.7% measured N); GPU attach + real SFT deltas DONE 2026-09-08: sft_v2_e1 pass@1 0.0000 / test_credit 0.0625 (best of all models), target 0.0/0.0, pilot 0.0/0.0312 - NO model executes generic tasks correctly (226M ceiling; ast 0.98 vs pass@1 0.0 = parses-not-runs; full forensics in research/training_analysis_2026-09-08.md) | scripts/mini_eval.py; scripts/status.py; runs/{sft_v2_e1,target,pilot}/final/mini_eval_report.json |
| 13 | Milestone D - Data upgrade for the NEXT pretrain (row 6 successor): gated sources + mix | `done` | User GO 2026-09-06 (window). DONE 2026-09-06: research/pretrain_mix_proposal.md (Exa-backed, 9 payloads in research/raw/milestone_d_*) - stack-v2 python impractical (233 GB dedup subset; Hub exposes blob-IDs only, needs AWS+SWH agreement), proposal = 30% the-stack-smol python + 55% starcoderdata python + 10% CSN python + 5% Evol-Instruct = ~134M train tokens at ctx 1024 (~230k rows), streaming-only download plan (~30-60 min realistic, 24.8 GB headroom left), 14-item acceptance checklist; prepare_data.py now auto-loads .env (gated unlock VERIFIED: the-stack-smol probe OK via HF_TOKEN). POST-M3: wire the 4-source chain (+ optional per-source target_rows) into the next-pretrain config, measure rows/tokens-per-row/dedupe per source, optional P-scale A/B. **WIRING+MEASUREMENT DONE 2026-09-09 (subagent A); RUN stays USER-GATED until the next pretrain window opens.** Wiring: per-candidate target_rows mix mode in prepare_data.py (ALL candidates fetched, ONE shared SHA1 set cross-source, source_stats.json additive in both modes; legacy 5-row regression BYTE-IDENTICAL sha256-verified; refusal/fallthrough/live-mix tests PASS) + data_dir passthrough - BIG FINDING: the-stack-smol AND starcoderdata lost their 'config: python' builder configs (only 'default' remains); data_dir form verified; pilot.yaml/pilot_b8.yaml fixed same day. configs/next_pretrain.yaml: pilot dims @ctx 1024, adamw_bnb_8bit b1/a32 (Milestone B default), save=eval=100, 19/19 validator PASS. MEASURED 2k rows/source (rev 2): smol 2717.2 / starcoder 2345.7 / csn 271.8 / evol 474.4 tok/row (whole-file sources ~4x above the 700/600 ests = FAIL; csn/evol PASS) - the proposal's row targets = 3.75x the 134M budget on real rates. Cross-source SHA1 dedupe 8,000 rows = 0.0000 (CSN<->starcoderdata overlap NOT observed at 2k scale). Gate incident: 403 with VALID token = terms never accepted (gated=auto grants immediately; user accepted 2026-09-09 -> re-measured). SIZING (user-delegated 'decide after re-measure'): token-proportional smol-capped 10000/37500/59000/16900 rows = ~133.65M kept tokens (~20/63/11/6% shares), max_steps 4100 consumes ~98% | research/milestone_d_actual.json (rev 2); research/milestone_d_measurement.md; configs/next_pretrain.yaml; scripts/prepare_data.py; scratch data/md_measure/ + data/legacy_check/ |
| 14 | Milestone E - Distillation path: tier-order decision + Tier 2 judge rubric | `done` | DONE 2026-09-06 (user GO window): decision framework written - recommendation Tier 3 BEFORE Tier 2 V1 (risk-ordered: same-tokenizer KL is clean + cheap, judge harness gets built in parallel, a KD'd S becomes the control that makes Tier 2's rerank delta measurable) with explicit reversal criteria; the decision itself executes at Tier 1 pass (user-owned); Tier 2 V1 judge contract FROZEN: strict-JSON verdict schema {verdict pass|partial|fail, score 0-2, reason}, 5-step parse ladder ending in ast.literal_eval fallback (never crashes a run; unparseable -> ast baseline), jsonl verdict logging + agreement rate + unparseable-rate gate (<10%), temp 0.2 / top_p 0.9 / max 120, teacher inference always STANDALONE | research/c12_tier_order_decision.md |
| 15 | Milestone F - Off-site backup of finals to a private HF Hub repo | `blocked` | BLOCKED on user: private HF Hub repo name (upload = external side effect needing approval). READY 2026-09-06: scripts/backup_to_hub.py - dry-run by default, token explicitly loaded from .env (repo pattern; hf_hub does not read .env), dry-run PASS (whoami ahmadsyy; runs/pilot/final manifest 7 files / 387.8 MiB; training_args.bin + optimizer.pt + checkpoint-* correctly excluded); real upload needs --repo <user>/<name> --execute and stays user-gated | scripts/backup_to_hub.py; MEMORY LFS row |
| 16 | Milestone G1 - S-scale hybrid-arch sandbox (GDN/Flash-Next validation at 12M) | `done` | Design DONE 2026-09-06 (user GO window): research/gdn_sandbox_design.md (Exa-backed, payload-cited: chunked delta-rule recurrence, FLA fp32-internal cast as THE fp16 stability workaround, 3:1 GDN:gated-attn ratio x3 independent sources, attention sink 46.7->4.8%) - triton-free pure-PyTorch port path identified (Megatron-LM torch_chunk_gated_delta_rule deterministic mode over FLA kernels); S-scale validation protocol = sanity_check -> 200-step train -> eval vs param-matched arch:gqa control, 3 gates to earn a P-scale trial. IMPLEMENTATION POST-M3 ONLY (src/model.py untouched while M3 live - auto-resume contract). **IMPLEMENTED 2026-09-09 (subagent B, rows-13/16 session)**: src/gdn.py = pure-PyTorch torch_chunk_gated_delta_rule port (chunk 32, WY + fp32 solve_triangular, ALL state math under a disable-autocast fp32 guard, conv k=4, RMSNormGated; GA = GQA-SDPA + per-head sigmoid gate; layer layout hard-bound [GDN,GDN,GDN,GA] - design's modular formula would have made every layer GA) + src/model.py gdn_hybrid dispatch with an idempotent AutoConfig/AutoModelForCausalLM registration (transformers 5.16.1 auto_map is DOUBLE-BROKEN: trust_remote_code hard gate + 2-part path parser; registration fires via eval.py's existing src.model import - fresh-process save->from_pretrained->generate roundtrip PROVEN, 60/60 weights) + configs/gdn_smoke.yaml/gdn_smoke_ab.yaml (control ffn 1352; hybrid 13,331,952 vs control 13,330,688 params = +0.0095%; both reuse data/smoke corpus for identical-data A/B); CPU math equivalence 13/13 PASS (chunked==naive, rel <= 4.8e-6). GATES ALL PASS (completed 2026-09-10 ~00:47 local = 2026-09-09 21:47 UTC, orchestrator-owned GPU queue after the concurrent Track A matrix cleared): **G1.a** sanity 4/4 PASS BOTH arms (hybrid peak 0.30 GB at ctx 256/bs2, finite loss 10.6552, grad_norm 11.13; smoke regression peak 0.29 GB - gqa path untouched). **G1.b** hybrid 200-step train PASS (eval/loss 5.0038@50 -> 3.9557@100 -> 3.6498@150 -> 3.5934@200 monotonic, <= 4.0 gate; train/loss 329 -> 116.7) + **KILL/RESUME DRILL PASS** (killed at step 103 after ckpt-100 saved; relaunch resumed at exactly 101/200; train_summary resumed_from checkpoint-100; eval curve continuous across the boundary). Control arm gdn_smoke_ab: 4.5795. **G1.c** eval.py both finals: hybrid val_loss 3.6040 / ppl 36.75 vs control 4.5795 / ppl 97.47 -> DELTA -0.9755 <= 0 **GATE PASS** (hybrid generations locally-syntactic is_prime/Stack fragments vs control underscore-degeneration). fp32-state cost ~1 MB of 0.30 GB peak (<0.5%; extrapolates far under the 15% P-scale bound). Design 3.2 acceptance items 1-4 ALL PASS -> row-16 deliverable MET; P-scale trial = separate user decision (row 6). Honest wording note: the design's 'val ppl <= 4.0' gate reads as eval_loss <= 4.0 (its own random-init ~12 reference is a loss; M0 smoke was 4.95) | research/gdn_sandbox_design.md; src/gdn.py; scripts/test_gdn_math.py; runs/gdn_smoke{,_ab}/final/{train_summary,eval_report}.json |
| 17 | Milestone G2 - One-command custom pipeline (run_custom) | `done` | DONE 2026-09-06 (user GO window): scripts/run_custom.py - sanity_check -> prepare_data -> tokenize_data -> train -> eval with stop-on-fail, per-step timing + runs/<phase>/custom_chain.json report, --from resume, --dry-run; never-co-run guard: nvidia-smi compute-apps FILTERED to python processes (Windows WDDM quirk: every desktop app with a GPU context is listed - unfiltered it would abort every chain; filter verified live: shows ONLY the M3 trainer python PID, guard would correctly abort before train/eval); first real chain run post-M3 (custom_example) | scripts/run_custom.py; configs/custom_example.yaml |

| 18 | Milestone B GPU execution — 8-bit Adam A/B arms + LoRA hook + vram_probe (GPU window open post-M3/T1) | `done` | DONE 2026-09-07: arm A eval 2.5644 (2,801 tok/s, probe 1.91 GB); arm B 8-bit eval 2.5603 (2,854 tok/s, probe 1.36 GB, resume drill PASS ck251); arm C b2/a16 eval 2.5604 (1,680 tok/s, probe 1.56 GB, pace FAIL -> rejected). All gates measured; **8-bit Adam b1/a32 = default for next pretrain** (row 11); LoRA hook GPU-probed 0.72 GB peak / 2.61M trainable (row 10 GPU part DONE). GPU queue now hands to rows 19 (SFT v2 pilot) / 20 (KD baseline) | research/milestone_b_8bit_ab.md; runs/pilot-{ab-base,b8,b8-r2x16}/final/train_summary.json |

| 19 | SFT v2 - minimax3 distillation corpus (26,469 pairs / ~2.1M est tokens) from runs/target/final | `done` | **DONE 2026-09-07/08 - DELIVERABLE = runs/sft_v2_e1/final, BOTH GATES PASS.** Data prep: combined/all_train.jsonl 25,146 rows (instruction+response) -> min_chars 30 gotcha (T1's 200 floor dropped 81% of the corpus) -> kept 19,252 (18,752 train + 500 val; drop_long 115, drop_ast 0). PILOT PASS: eval 0.3989, ast 0.94/0.92, forgetting +3.7%. Full 2-epoch (runs/sft_v2, 2,344 steps, eval 0.2421): ast 0.98/1.00 BUT forgetting +19.7% = gate FAIL (honest; no knee at late ckpts) -> e2 kept as overfit evidence. **e1 fix (epochs 1, 1,172 steps): ast greedy 0.98 / sampled 0.96, forgetting CSN 2.0466 = +9.8% WITHIN gate (<= +10-15%)** - beats Tier 1 on both axes (T1: 0.86/0.88, +12.6%). GOTCHAS: (1) finals had been de-weighted (disk cleanup) -> restored bit-exact from runs/target/checkpoint-4000 (best@4000 per trainer_state); (2) sleep froze the run + killed a mid-write checkpoint (Kernel-Power 506/507) -> partial-ckpt guard had NO EFFECT because scripts passed resume_from_checkpoint=True (HF internal discovery ignores it) -> FIXED: all three scripts now pass the PATH (MEMORY 14); (3) disk filled to 3.9 MB free -> checkpoint writes hang (E: healthy; scratch ate ~28 GB); cleaned MY OWN post-run scratch + e1 superseded ckpts (~13 GB freed); e2 ckpts + A/B arm ckpts KEPT pending user cleanup decision (row 2). Follow-up option (user-gated): LoRA-SFT on frozen base via the row-10 hook = near-zero forgetting variant | data/distillation_data/minimax3/; configs/sft_v2.yaml; configs/sft_v2_e1.yaml; data/sft/minimax3/; runs/sft_v2{,_e1}/final/eval_report.json |
| 20 | C12 Tier 3 - intra-ladder KD, same-tokenizer (BEFORE Tier 2 per row 14 frozen decision) | `done` | **DONE 2026-09-08 - READ-OUT: KD BEATS THE BASELINE AT EVERY POINT.** Implementation: scripts/kd.py (frozen teacher runs/pilot/final 100.7M -> student 4L/256 ~12.3M on pilot shards; loss = 0.5*KL(teacher||student, tau=1) + 0.5*CE; eval stays PURE CE for A/B validity; auto-resume incl. partial-ckpt guard + path-pass). Fair A/B (identical data/steps/seed, batch 8, 2,000 steps): baseline 2.6201 vs **KD 2.4755** (-0.1446, -5.5%); at 1/3 steps (500): baseline 3.5325 vs **KD 3.4142** -> plan §5 criterion "distilled >= baseline at <= 1/3 steps" **PASS -> the ~1/10 claim transfers to this hardware; future rungs can be distilled instead of pretrained from scratch**. Cost: baseline ~3 min GPU (!), KD ~35 min (teacher forward dominates; the plan 2-4 s/it est was off for S-scale on this GPU). NOTES: one sleep-freeze mid-run (Kernel-Power 506/507 again; run resumed via the resume contract); KD checkpoints tiny (~150 MB each). NEXT per plan: Tier 2 V1 judge-rerank = user-gated (teacher Qwen3.5-0.8B downloaded) | research/c12_tier_order_decision.md; research/c12_distillation_plan.md §5; scripts/kd.py; runs/kd-s-{baseline,t1}/train_summary.json |

| 21 | Web UI (Gradio) — chat + data/config + train launch + monitor, per WEBUI_PRD.md | `done` | **U1-U5 COMPLETE — full e2e PASS 2026-09-08** (user go). U1 dashboard; U2 chat (VRAM policy all 3 branches verified live incl. GPU 696 MiB + real gen; picker lists only weights-bearing dirs); U3 monitor (plot/ETA/GPU/log 10 s refresh + U5 eval report card w/ mini_eval_report.json fallback); U4 train launcher (webui_<name>.yaml + preflights + run_custom chain, NO kill button, job_state.json); U5: --lan / --auth USER:PASS (gradio login flow — unauth = login page + 401 API, cookie flow verified by content) / --webhook or $WEBUI_WEBHOOK_URL notifications (gr.Info toast + best-effort POST 5 s; events run_finished AND run_crashed both received by a live receiver) / Monitor Danger-zone checkpoint delete (traversal-safe, refuses live run + chat-loaded ckpt, typed confirm, freed-MB report). E2E PASS: launch → sanity 4/4 CUDA → prepare 1470/30 rows → tokenize 2589 blocks → train 100 steps (best_eval 6.1545) → eval → webhook run_finished → eval card rendered → e2etest/final chat-able on CUDA; boot check HTTP 200. 3 integration bugs found+fixed during e2e: (1) _job_alive psutil-absent → ctypes OpenProcess; (2) app preflight own-pid CUDA false positive after chat unload (filter own pid + separate cuda-model block); (3) run_custom GPU-busy false positive — Windows venv python.exe is a launcher shim (spawns real interpreter as child), so getppid() = own shim, NOT the CUDA-holding webui/probe grandparent → _ancestor_pids() Toolhelp32 walk (exact PROCESSENTRY32W layout; wrong field width silently breaks dwSize) + _pid_alive() stale-entry filter + abort msg now prints pids; real co-run protection unchanged. Also fixed: matplotlib fig leak per 10 s tick. HONEST NOTES: toast visual unverifiable headless; crash notification verified via headless dead-pid probe, not a real crashed train; delete-while-chat-loaded guard code-reviewed only (live + traversal guards tested live/headless) | WEBUI_PRD.md; webui/app.py; scripts/run_custom.py; README |
| 22 | Web UI U6 - hardening & resume UX: one-click Resume, resume-collision guard, `--lan` auth gate, 8-bit optim default, chat length guard, chain-step indicator, picker refresh, job banner, tok/s in Monitor | `done` | **DONE 2026-09-08 - ALL PASS GATES MET.** One-click Resume (Monitor, same config + zero flags, verified by kill->Resume drill: UI-launched u6drill 600 steps 8-bit -> ckpt-500 -> taskkill tree -> Resume -> completed, summary resumed_from: checkpoint-500, eval 2.348->2.3446 continuous); resume-collision guard (regenerated config differs -> refuse; identical -> "unchanged (resume-safe)"); --lan w/o --auth exits 1 w/ clear msg; 8-bit optim = Train dropdown default (drill peak 940 MiB vs 1.91 GB fp32); chat truncation warn @ ctx-max_new (2801->240 tok test PASS); chain-step indicator + chain_out.log tail in Monitor; chat picker refreshes every 15 s; job banner (phase/pid/started/ETA) on Dashboard+Train+Monitor; tok/s+MFU line in Monitor. GOTCHA: stale UI instances hold CUDA contexts -> UI launches abort at train step (never-co-run) -> restart the UI first. Drill artifacts deleted; UI restarted (pid 21284). | WEBUI_PRD.md 5 U6; webui/app.py |
| 23 | Web UI U7 - data & keys: Settings key manager (gitignored .env, masked, override/delete, path shown), stream-vs-local-cache data modes, preflight disk estimate + rows cap, gated-key prompt | `done` | **DONE 2026-09-08 - PASS GATES MET (one user-gated).** Settings tab key manager over gitignored .env (mask = last 4 only; add/override/delete verified on disk incl. EXA_API_KEY preservation; unknown keys refused; gitignore check rendered). Data-mode radio (Stream default / Download full cache) -> configs data.data_mode -> prepare_data.py honors both. Rows hard cap 500k + preflight disk estimate (raw ~0.8 KB/row, tokens ~0.8 KB/row, ~3 rotated ckpts by preset, download x3 cache factor). Gated w/o key: the-stack-v2 probe -> "[gated dataset - add HF_TOKEN ... Settings tab]" (with-key path USER-GATED: needs real HF_TOKEN; .env auto-load mechanism is the existing prepare_data pattern, unchanged). Stream e2e from UI: TinyCode 1000 rows -> 100-step 8-bit train -> best_eval 5.8958, 232 s. Layout gate: download-mode wikitext probe = same train_NNN/val_NNN/meta.json schema as stream e2e (shard count is a shard_tokens knob, not a mode artifact). BUGS FOUND+FIXED: (1) prepare_data download mode indexed ["train"] AFTER split="train" -> column-select ValueError (fix: drop the index); (2) e2e first attempt failed at 1,250/2,000 rows - TinyCode corrupt shard ALSO kills full download (DatasetGenerationError @ ~1900 ex) - stream with row caps below the shard works (MEMORY 17); the-stack-smol is now UNGATED. Probe/UI fixtures deleted; UI restarted pid 18616. | WEBUI_PRD.md 5 U7; webui/app.py; scripts/prepare_data.py |
| 24 | Web UI U8 - transparency: dataset preview (first N rows + drop counts), advanced YAML disclosure + generated-YAML preview, per-ckpt MB in Danger zone, runs/ footprint, multi-run loss overlay | `done` | **DONE 2026-09-08 - ALL 3 PASS GATES VERIFIED.** (1) Preview counts match prepare: TinyCode 300 rows -> 305 scanned / 5 min_chars dropped / 300 kept (same text_of + min_chars + sha1-dedupe rules, same stop-at-kept condition, window capped at 2,000 scans; local-file + missing-file paths verified). (2) YAML preview equals the written file: pre_btn wrapper reads configs/webui_<name>.yaml back into a read-only gr.Code (922-byte config round-tripped byte-identical). (3) Overlay renders 2 phases: kd-s-baseline + kd-s-t1 eval/loss, 2 lines + legend. Plus: per-ckpt MB labels in Danger zone (value stays bare folder name, delete flow untouched - verified 145 MB ckpts), runs/ footprint on Dashboard (31.28 GB / 13 runs, top-5 listed). UI restarted pid 25144. | WEBUI_PRD.md 5 U8; webui/app.py |
| 25 | Web UI U9 - chat streaming (TextIteratorStreamer) + generation-level stop | `done` | **DONE 2026-09-08 - PASS GATES VERIFIED.** chat_generate replaced by chat_stream generator: TextIteratorStreamer(skip_prompt=True) with model.generate on a daemon thread; yields progressive text. Stop = explicit "Stop generation" button + gradio client-cancel both route through _STOP_EVENT -> StoppingCriteria halts generate at the next token step (never process kill); stream loop finally-block sets the flag so the generate thread always winds down. Verified: GPU 65 progressive yields + stop at 1.0s (248-char partial kept) + model reusable after stop (gen3 OK); CPU-fallback path same code (49 yields, stop OK, reuse OK); model unloaded after test. UI restarted pid 11904. | WEBUI_PRD.md 5 U9; webui/app.py |
| 27 | Web UI U11 — cooperative stop-at-next-checkpoint flag (train.py/sft.py save-boundary check) + browser notifications + HTTPS option | `pending` | Approved 2026-09-08; amends the PRD no-kill rule (checkpoint-aligned clean exit ONLY, never a process kill). Runs LAST. NEXT: user go | WEBUI_PRD.md §5 U11; PLAN §5.3 (contract intact) |
| 28 | Distill + context research survey (user request 2026-09-08): KD methods (Qwen/DeepSeek/GKD/DistiLLM/TM on-policy), cloud-only (API/text) distillation taxonomy, per-stage data recipes, context extension + in-model memory, Sept-2026 landscape | `done` | **DONE 2026-09-08 - research only, zero GPU.** 22 Exa tasks -> research/raw/{kd*,stg*,ctx*,chk*,ans*}; 4 parallel subagent notes + synthesis with ranked user-gated adoption plan (A context eval-probes ~1-2 h -> B YaRN@2048 ~2-4 h -> C T-as-teacher KD + skew-KL ~3-4 h -> D Tier 2 V1->V2b SODA -> E stage/LR fix for row 13 -> F cloud CoT corpus). Key: distill>RL for small students (Qwen3 TR + R1); KV cache NOT our bottleneck (16 KiB/tok, 512 MiB @32k - positional extrapolation is); SODA = feasible-now on-policy black-box; HQ-last under decay-to-zero is suboptimal (LR fix: end ~1/3 peak or CMA); DeepSeek-V4 VERIFIED (Pro 1.6T/49B, Flash 284B/13B, 1M ctx, Muon); "Qwen3.8 the Last/Next-Flash" names UNVERIFIED (Flash-Next=Qwen4 preview, existing doc). NEXT: user picks track(s) | research/distill_survey/REPORT.md; research/distill_survey/notes/; exa_tasks_round6.json |
| 29 | Track A - context eval probes (Dynamic-NTK + StreamingLLM, eval-only, no training) | `done` | **DONE 2026-09-09 (user go): DECISION = Track B targets ctx 4096 (YaRN factor 4); 2048/factor-2 fallback if the 8-bit-Adam vram_probe @4096 OOMs.** Knobs: src/model.py eval-only helpers (apply_rope_scaling/reset_rope_scaling = HF native dynamic rope; build_streaming_sink_mask; streaming_position_ids) + eval.py flags (--ctx/--rope-scaling/--rope-factor/--stream-window/--sink-tokens/--stream-positions/--batch/--quick/--skip-gen/--report-out/--label; config eval.* mirrors) - defaults OFF, sanity_check 4/4 PASS, scripts/ctx_probe.py --selftest 5/5 PASS. Matrix runs/ctx_probes/20260909T184503Z (~2.1 h GPU, 0 OOM; full 978,944-token CSN val per point): target/final base 1.8512 (= trainer best@4000; on-disk eval_report 1.8641 is STALE pre-restore, MEMORY 31) -> ntk_2048 1.7740 (-4.17%), ntk_4096 1.8836 (+1.75%), noop_2048 +12.3%, noop_4096 +55.8%, w1024s4abs_2048 1.8392 (-0.65%); e1 base 2.0466 (= recorded, clean) -> ntk_2048 -3.43%, ntk_4096 +3.90%, abs +0.26%. KEY FINDING: StreamingLLM pos_shift remap collapses at every W (W512 > W1024, sink 4 ~= sink 0, +70-126%) -> no attention-sink specialization in this from-scratch model; mask+absolute positions is the only working window variant (inference-time lever). VRAM 4.39 GB @1024/2048, 2.63 GB @4096 | research/distill_survey/track_a_result.md; runs/ctx_probes/20260909T184503Z/summary.json |
| 30 | Track B - YaRN context fine-tune (TARGET ctx 4096 per row 29 decision; 2048/factor-2 fallback) | `pending` | User-gated; PREREQ row 29 DONE 2026-09-09 - plan §A gate FIRED (NTK@2048 -4.17%/-3.43% eval-only): B now targets 4096, YaRN factor 4, original_max 1024; MUST vram_probe BOTH @4096 and @2048 with 8-bit Adam first (6 GB card; 4096 training may OOM -> fallback); ~500-1k steps on existing target tokens re-packed at the train ctx, NEW run dir (base runs/target/final never touched in place); gates: val lift on BOTH surfaces (e1@4096 eval-only is +3.9% - fine-tune must lift instruct too), forgetting guard @1024, honest beyond-ctx extrapolation report | research/distill_survey/track_a_result.md; adoption_plan.md §B; notes/04 §1 |
| 31 | Track C - T-as-teacher KD (same tokenizer) + DistiLLM skew-KL A/B | `done` | **DONE 2026-09-09 - GATE PASS, plain-KD ADOPTED, skew-KL REJECTED (3 arms, ~3.5 h GPU, 0 OOM, 0 interruptions).** Recipe: teacher = frozen 226.5M runs/target/final (fp32 resident); student = P-arch 12L/768 = **100.68M** (not the plan's ~110M estimate - honest number); data/target/tokens CSN ctx 1024 (46811 train / 956 val blocks); 2000 steps; batch 4 x accum 2 (effective 8); eval+save 100; adamw_bnb_8bit (row 11 next-pretrain default, SHARED by all arms -> internally fair; deviation from row 20's adamw_torch recorded); lr 4e-4 cosine warmup 100; seed 42. **RESULTS (curves committed at runs/kd-t2p-*/final/eval_curves.json):** baseline 2.9453 @2000; plain-KD **2.7476** @2000; skew-KD 2.8104. KD ahead of baseline at EVERY matched step: 100 -2.0%, 500 -2.5%, 600 **-3.05%** (the plan §5 gate at <= 1/3 steps -> **PASS**), 1000 -4.0%, 1500 -6.4%, 2000 **-6.71%** -> the row-20 distilled-rung claim transfers to this T(226.5M)->P(100.7M) rung despite only 2.25x compression (row 20 was 8.2x); runs/kd-t2p-kd/final = the candidate distilled-P rung. skew-KL (DistiLLM alpha-SKL lambda 0.1, CPU-validated exact vs brute mixture-KL diff <= 2.4e-7): +2.29% vs plain-KD @2000, led only at 800-1000; both KD arms' end grad norms healthy (1.0-1.6) -> no fp16 instability for the skew floor to fix -> NOT adopted; skew_lambda=0 = byte-identical legacy default. VRAM ladder (fp32 KD logits dominate): b8 7897/8192 (96%) -> b4/a2 full-cycle 7010 (86%) chosen for both C1 arms; b2/a4 train 4926 but eval_batch-8 transient spiked 7486 (eval stays at 4); skew chunked 7447 (91%). GOTCHAS -> MEMORY 25 (F.kl_div target must be probs or log_target=True - NaN otherwise, caught by the CPU test BEFORE GPU; per-shape 50-step VRAM probes; run_code 600 s ceiling caps WAIT loops too). Machine moved back to MUO4QK5 (RTX 4000 8 GB, driver 595.97) - ENVIRONMENT.md updated. Residue (user decision): 3x 403 MB fp32 arm weights + rotated ckpts (~1.5 GB/arm) on disk, E: 41.1 GB free - keep runs/kd-t2p-kd/final at minimum | research/distill_survey/adoption_plan.md §C + RESULT addendum; scripts/kd.py (skew_lambda config-gated); runs/kd-t2p-{baseline,kd,kd-skew}/final/{train_summary.json,eval_curves.json}; TASKS row 20 | RECIPE: identical data (data/target/tokens CSN ctx 1024, 46811 train/956 val blocks) / 2000 steps / seed 42 / adamw_bnb_8bit (row 11 next-pretrain default, both arms share it -> internally fair; deviation from row 20's adamw_torch recorded) / eval+save 100. VRAM probe ladder (KD arm): micro-b8 7897/8192 MiB (96%, OOM-risk on the fp32 KD logits) -> b4/a2 FULL-cycle peak 7010 (86%) = chosen for BOTH arms (A/B fairness needs identical micro-batching; b2/a4 train only 4926 but its eval_batch-8 transient spiked 7486 -> eval stays at 4). C2 = skew-KL 0.1 config-gated in kd.py (default 0 = byte-identical plain path; CPU math validation PASS: exact vs brute mixture-KL at skew {0.1,0.3,0.9} diff <= 2.4e-7, monotone, finite grads; F.kl_div log_target=True gotcha caught by the CPU test BEFORE reaching kd.py) -> 50-step VRAM probe then full arm after C1 read-out | research/distill_survey/adoption_plan.md §C; scripts/kd.py; TASKS row 20 |
| 32 | Track D - Tier 2 V1 judge-rerank -> V2b SODA preference pairs | `pending` | User-gated; V1 contract (row 14) frozen; V2b = same one-time snapshot -> winner SFT + stored pairs (pair-weighted SFT; true DPO blocked on missing trl); 2k prompts x 8 candidates, AST gates, judge STANDALONE | research/distill_survey/adoption_plan.md §D; c12_tier2_brief.md; notes/02 §4 |
| 33 | Track E - stage/LR revision for NEXT pretrain (row 13 successor) | `pending` | User-gated; bulk ~80M / mid-train ~27M (Evol 10-12%) / anneal ~27M; MUST: cosine_with_min_lr (end ~1/3 peak) or constant-LR + CMA (2511.18903/TREC finding); knee-stop ~1 epoch past the eval knee (M3 spent 2000 steps for +1.7%, training_analysis §3.1); optional MTP depth-1 500-step probe arm; runs in the next pretrain window | research/distill_survey/adoption_plan.md §E; notes/03; pretrain_mix_proposal.md; training_analysis_2026-09-08.md |
| 34 | Track F - LoRA-SFT variant of SFT v2 (analysis #2 recommendation) | `pending` | User-gated; same minimax3 corpus, frozen base via the live row-10 hook (0.72 GB measured @ 2.52% trainable); config-only, no src changes; predicted forgetting ~0-2% vs e1's +9.8%, ast somewhat below 0.98; ~1-2 h GPU; if near-e1 ast -> becomes default SFT recipe | research/distill_survey/adoption_plan.md §F; research/training_analysis_2026-09-08.md §5.2; TASKS row 10 |
| 35 | Track G - SFT v3 mixed-corpus (analysis #3, anti-repetition-loop) | `pending` | User-gated; Evol + minimax3 + generic-phrasing slice -> widen the distribution e1 loops on; 1 epoch, LR re-derived per density lesson, pilot-first; CPU data prep (co-run safe) + ~1 h GPU; shares the mixed prompt pool with Track D; gates: three-surface (ast + CSN guard + mini_eval), qualitative prompts must improve | research/distill_survey/adoption_plan.md §G; research/training_analysis_2026-09-08.md §1.2/§5.3; configs/sft_v3.yaml (new) |
| 36 | Track H - agent-side memory experiment (rolling summary + fact store, zero training) | `done` | **RESOLVED 2026-09-09: the row-37 Phase 1 rerun PASSED the recall gate (store arm 3/6 vs required >= 2; baseline 1/6)** - gate now green on runs/h2_copy_lora/final; honest mechanism deltas (summarizer 0 folds, QA-template code tautologies) recorded in row 37 + adoption_plan addendum. Baseline: 3 GPU iterations DONE 2026-09-08 on runs/sft_v2_e1/final (iter1 QA@48tok 1/6; iter2 QA+format-cue@96 0/6 — snapshot runs/agent_memory_h/iter2_qa_cue/; iter3 QA@96 + needle-probe@96 1/6 qa / 0/6 probe — f3 deterministic twice). MECHANISM VALIDATED: store 6/6 (junk-filtered), retrieval 6/6 correct fact per question, model-mode rolling summary 7/7 folds (61 tok), regression PASS (ast 4/4 preamble vs 2/4 plain), overhead 61 + ~14.5 tok = ~7.4% of ctx. GATE FAIL: copy-out of injected facts 1/6 < required 2 — the student continues QA with import boilerplate (MEMORY lesson 18). BLOCKED ON USER -> DECIDED 2026-09-08: user diagnosis = models are Python-only (corpus distribution), fix = expand knowledge via distillation -> row 37 (H2 copy-behavior + teacher-distill ladder). This row stayed blocked until the row-37 rerun passed the recall gate - it did (2026-09-09) | runs/agent_memory_h/report.json; runs/agent_memory_h2_rerun/report.json; scripts/agent_memory_eval.py; adoption_plan.md Track H RESULT note + 2026-09-09 addendum |
| 37 | Track H2 - fix the recall wall: copy-behavior SFT (phase 1) + teacher-distilled general QA (phase 2) | `done` | **PHASE 1 DONE 2026-09-09 - DECISIVE GATE PASS** (10/10 H2_RESUME ladder steps). Corpus: 20 background subagent chunks (30 pairs each; slices 1-4 = personal / technical-ops / logistics / note-completion) -> 600 raw pairs, 2 chunks needed lossless format repair (real newlines in JSON strings; missing opening quotes) -> official validator PASS **585/600 kept** (15 drops: response > 2 sentences) -> data/sft/h2_copy/pairs.jsonl. Config fixes (sft_data gotchas, MEMORY 22): min_chars 30->4 (it floors the RESPONSE - killed every completion answer), rows 5000->556 (n_val computed from TARGET rows -> 250 val/110 train inverted split -> now 28/556); T dims mirrored into the YAML for sanity_check (sft.py loads dims from the checkpoint - training behavior unchanged). sanity PASS (LoRA trainable 4,849,664 = 2.096%); pilot PASS (19 steps, eval 3.161); FULL SFT PASS (556 pairs x 2 epochs, 70 steps ~7 min, eval 2.3612 -> 2.0944 -> **2.0680**). Code guards PASS: CSN **2.0492** vs e1 2.0466 = **+0.13%** (gate <= +10%); ast greedy **0.90** >= 0.85 (sampled 0.82 vs e1 0.96 - honest note below the greedy gate). **DECISIVE RERUN PASS (step 9)**: gate_recall store arm **3/6** (required >= 2; baseline 1/6) - probe 3/6 (rust, layla, neovim), qa 2/6, controls 0/6; retrieval 6/6 correct. HONEST DELTAS: Track H regression probe 0/4 ast in BOTH arms (gate = preamble >= plain -> PASS, but QA-template code-gen collapsed to copy-tautologies; eval.py ast 0.90 on the SFT template still passes); summarizer 0 folds (was 7/7, 61 tok) - mechanism now extract/store/retrieve/copy only, overhead 1.3% of ctx; 2 of 3 recall misses = prompted extractor corrupted the stored fact (March 15 -> "ISO 15", wifi value echoed into "the wifi password"), 1 = copy-out failure (f2). **PHASE 2 DONE 2026-09-09 - ALL 3 GATES PASS.** USER DECISIONS: dataset path (no local teacher; NOT Qwen3.5-0.8B), deterministic extractor arm ADDED, OpenHermes-2.5 GPT-4 slice sanctioned mid-build ("more powerful datasets"; HF token in .env). Corpus: +947 QA kept (371 dolly open_qa CC-BY-SA + 223 no_robots OpenQA/Brainstorm/Generation CC-BY-NC [Chat = multi-turn by construction, 795/796 skipped] + 353 OpenHermes-2.5 ShareGPT self-contained) + 585 copy verbatim = 1532 mixed; sft kept 1530 (train 1457/val 73). Training: configs/h2p2_mixed_lora.yaml (LoRA r=16 from e1, Phase 1 recipe); sanity PASS (2.096%); pilot PASS (19 steps, eval 5.590); FULL PASS (184 steps 1151 s, eval 5.36 -> **4.9461** best=final). GATES: CSN **2.0077 vs e1 2.0466 = -1.9% (improved)**; ast greedy **0.86 >= 0.85** (sampled 0.78, down from 0.82); Track H rerun (fresh --out): prompted arm **2/6** (probe rust+neovim, qa neovim; control 0/6) AND deterministic arm **2/6** (probe rust+layla, qa pineapple42; control 0/6) - both >= 2 = PASS; deterministic store VERBATIM-PERFECT (6/6 scripted facts byte-exact incl 'March 15' + 'Pineapple42' - the exact 2 the prompted extractor corrupted in Phase 1; f6 wifi qa flipped to PASS, f5 flipped to a copy-out miss) -> extractor corruption FIXED, remaining wall = pure copy-out; regression gate PASSes trivially (0/4 = 0/4 both arms); summarizer 0 folds (mechanism = extract/store/retrieve/copy, overhead 1.0-1.4% ctx). HONEST vs Phase 1: recall 3/6 -> 2/6 (copy share 100% -> 38% cost ~1 point), ast greedy 0.90 -> 0.86, sampled 0.82 -> 0.78, canned-prompt tautologies persist (43/50 greedy) - the QA mix did NOT visibly cure the collapse at this LoRA scale, but CSN improved. Follow-up -> row 38 (user-gated) | research/distill_survey/H2_RESUME.md; research/h2_corpus_spec.md; data/sft/h2_copy/pairs.jsonl; configs/h2_copy_lora.yaml; runs/h2_copy_lora/final (+eval_report.json, train_summary.json); runs/h2_copy_lora_pilot/; runs/agent_memory_h2_rerun/report.json; TASKS row 36 |
| 38 | Track H2 verdict follow-up: accept 2/6-threshold recall + residual QA-tautologies, or stronger-student path | `done` | **DECIDED 2026-09-09 (user pick from the 4-option question menu): next track = Track C (trained path KD)** - NOT accept-as-shipped, NOT a higher-copy-share variant, NOT a stronger-student SFT. Row 31 owns execution. Original gate - User-gated (H2_RESUME section 6 options): the 2026-09-09 Phase 2 result has ALL gates green but recall sits AT the 2/6 threshold in both extractor arms (Phase 1: 3/6), ast greedy slipped 0.90 -> 0.86, and canned-prompt tautologies persist - options = accept as shipped / another mixed-corpus variant (higher copy share, e.g. 585 copy + ~450 QA) / stronger student (bigger arch or full FT) / Track C trained memory. Do NOT loop more SFT variants without the user | adoption_plan.md Track H addendum 2; runs/agent_memory_h2p2_{prompted,deterministic}/report.json |

## Done recently (context)

- M0 smoke PASSED (kill/resume drill) · M1 VRAM probe PASSED (target approved,
  4.24/4.4 GB @ seq 512 and 1024) · M2 pilot PASSED (final ppl 3.19) — HANDOFF §2.
- C12 Tier 1 implemented + CPU-validated while M3 trains (2026-09-06).
- Post-M3 readiness audit + custom model/dataset support while M3 trains
  (2026-09-06): local-file sources in prepare_data.py (jsonl/json/txt/csv)
  and sft_data.py (+ generic instruction keys), sft.py --pilot output
  isolation, infer.py --sft instruct mode, configs/custom_example.yaml
  template (sanity_check 4/4 PASS, 100.7M as designed) — all CPU-tested;
  README documents both workflows.
- Window work 2026-09-06 afternoon: peft installed + LoRA design DONE
  (row 9; implementation = row 10, user-gated); Tier 2 teacher switched to
  Qwen/Qwen3.5-0.8B (verified + downloaded); exa-py installed, Exa helper
  mandated in AGENTS.md section 3 (EXA_API_KEY .env pending from user); 3 prep
  commits PUSHED to origin/main (user-approved).
- 2026-09-06 (same window): recommendation menu approved as MILESTONES ONLY
  (B/C/D/E/F/G -> rows 11-17) - created, not started; both API keys
  (.env EXA + HF) added by the user; teacher web-context rerun via Exa.
- M3 restarted fresh on MUO4QK5 (2026-09-06 10:56) — the TU09FBO handoff
  bundle never arrived (user call); prior partial run abandoned, its bundle/zip obsolete.
| 26 | Web UI U10 - SFT + LoRA launch path (Train tab) | `done` | **DONE 2026-09-08 - PASS GATES VERIFIED.** Train tab "SFT from checkpoint" accordion: run name + base checkpoint picker + LoRA checkbox + Pilot checkbox -> `build_sft_config` writes `configs/<name>.yaml` (base = picked ckpt, Evol-Instruct dataset, fixed instruct template, FT LR 3e-5, forgetting-guard eval kept via tokens_dir -> data/<base_run>/tokens; LoRA checkbox emits the peft block r=16/7 target modules; resume-collision guard like build_config) + a chat-lookup stub `configs/<name>_pilot.yaml`. Chain = run_custom.py --sft (sft_data CPU co-run safe -> sft under the never-co-run guard + auto-resume; --pilot passthrough), launched detached under the single-lock job_state mechanism. VERIFIED: UI-launched SFT pilot on smoke/final (12M, full FT) completed in 402s (sft_data 77s + sft 325s, eval_loss 4.197, 5000 pairs 1 epoch); phases sft_u10 + sft_u10_pilot appear in Monitor; pilot final chat-able in INSTRUCT mode (template from stub, 97 progressive yields); unit gates: config build (ctx from base max_position_embeddings), peft block, collision guard, name guards, single-lock blocks second launch. Banner now shows completed chains as finished (runs/<p>_pilot/final accepted). Cleanup: unit-test configs removed; sft_u10.yaml + stub kept as live evidence | WEBUI_PRD.md 5 U10; webui/app.py; scripts/run_custom.py |
| 27 | Web UI U11 - cooperative stop + polish (runs last) | `done` | **DONE 2026-09-08 - PASS GATES VERIFIED.** Cooperative stop: src/stop.py CoopStopCallback (checks output_dir/STOP at on_save ONLY - checkpoint-aligned) wired into train.py + sft.py Trainer(callbacks=[...]); relaunch startup clears the stale flag ([stop] cleared...). Monitor tab button "Request stop (cooperative)" writes runs/<phase>[_pilot]/STOP via request_stop() (job_state pilot-aware path); banner shows "Stop requested" when set + alive. E2E on sft_u11 pilot (smoke/final): flag written at step ~5 -> clean stop after checkpoint-250 at epoch 0.8, exit 0, valid ckpt; zero-flag relaunch resumed ([resume] found checkpoint-250 + stale flag cleared) and finished with NO loss bump (eval 4.207 pre-stop -> 4.197 final). Browser notifications: /api/job_done FastAPI route (demo.app pre-launch) + Blocks-load JS poller (20s, localStorage opt-in) + Settings Enable-notifications button (Notification.requestPermission via js=); fires on live->finished transition beside toast + webhook. Optional HTTPS: --ssl-certfile/--ssl-keyfile pass to demo.launch (untested live - no certs on this box). Cleanup: sft_u11 test runs/configs/job_state deleted. NOTE: chain_out.log loss lines are block-buffered (lag real step count) - mid-run triggers must use flush=True markers or timing | WEBUI_PRD.md 5 U11; webui/app.py; scripts/train.py; scripts/sft.py; src/stop.py |

| 39 | Web UI U12 - Model tab: Architecture Explorer (real-shape SVG graph from configs + safetensors HEADERS, two-level info, token trace) | `done` | **DONE 2026-09-09 - PASS GATES VERIFIED.** Model tab (nested: Architecture Explorer | Training Simulator). Explorer builds the real block graph live from configs/*.yaml OR any runs/ checkout (checkpoints included) — safetensors HEADERS only via safe_open (tensor data never read, weights never loaded, zero GPU). Two-level info: plain-English default + Technical toggle (tensor names/shapes, dtype gate F16/F32/BF16, KV-cache KiB/token, per-block config keys; real-header tensors listed for a checkpoint — verified model.embed_tokens.weight 32768x1024 F32 on target/final; projected param counts bit-exact, marked "(projected)"). Click path: gr.Dataset rows (the SVG diagram is a static poster after the sanctioned shim removal; bare gr.SelectData hint — Gradio 6.26 event-data injection verified live); guided tour walks every block (1.6 s dwell; bound to the live selection graph). Real tokenizer trace (CodeLlama 32k, real IDs verified). Unit gates 6/6 explorer + 6/6 artifacts; browser drill PASS (agent-browser, clicks verified in a live Gradio session); read-only + CPU-only verified | WEBUI_PRD.md §5 U12; docs/plans/2026-09-09-webui-u12-u13-model-tab.md; webui/explorer.py; webui/model_tab.py; webui/artifacts.py |
| 40 | Web UI U13 - Model tab: Training Simulator (real-run REPLAY: Pretrain/SFT/LoRA/KD, synced KD-pair curves, scrub/speed, pluggable curve source) | `done` | **DONE 2026-09-09 - PASS GATES VERIFIED.** Replay-only v1 (user choice; synthetic what-if + real-CPU-toy = FUTURE): replays THIS repo's real runs — curves/params/eval from tfevents + train_summary.json + eval_report.json, only the clock is fake; reads runs/ only, writes nothing, zero GPU/VRAM. 9 configured replays across 4 techniques (Pretrain smoke/pilot/target; SFT sft_t1/sft_v2_e1; LoRA h2_copy_lora/h2p2_mixed_lora; KD kd-t2p-kd/kd-s-t1); smoke + kd-t2p-kd drill-verified end-to-end. Stage cards per phase (Stream+filter -> Tokenize+pack -> Train loop -> Eval -> Checkpoint+rotation -> Final+eval report) + gauges (step, best eval, VRAM est anchored to measured probes, batch x accum seq count) + checkpoint events + disk-rotation slots. Controls: Load / Play / Pause / +1 tick / Restart / scrub / speed 60-3600x (1 step ~ 10 s real). KD loads its baseline pair: synced dashed curve + delta line (drill-verified "KD vs baseline @ step 100: baseline - KD = +0.1106 (KD ahead)"). 7/7 simulator unit tests; browser drill PASS (load -> play advance -> pause freeze -> finish 200/200 best eval 4.7926 -> end card); technique-switch stale-dropdown-value bug found and FIXED (57055b1) | WEBUI_PRD.md §5 U13; docs/plans/2026-09-09-webui-u12-u13-model-tab.md; webui/simulator.py; webui/model_tab.py |
