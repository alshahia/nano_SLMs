# TASKS.md — live task list

One row per task; the table is the current truth, rewritten in place as
states change. Statuses: `pending` → `in_progress` → `done`, or
`blocked` (always say what gates it). User-gated tasks do not start
without the user's explicit go. Evidence links make every status auditable.

Seeded from HANDOFF §8 (2026-09-06); TASKS.md owns live status from now on.

## Now

| # | Task | Status | Gate / next action | Evidence |
|---|---|---|---|---|
| 1 | M3 target run (226.5M, ctx 1024, 5000 steps) — monitor to completion | `done` | **COMPLETED 2026-09-07 ~08:16 UTC on TU09FBO**: 5000/5000, zero-flag auto-resume from checkpoint-4500 (restored from checkpoint_backup\checkpoint-4500.zip at 4501/5000); train_summary.json: best 1.8512 @4000, final 1.8641 @5000, env fingerprint git c16179c / torch 2.14.0+cu126. History of interruptions (each handled by re-running the exact train command — auto-resume, zero flags): fresh relaunch 2026-09-06 10:56 on MUO4QK5; sustained ~6.5–7.9 s/it → ETA ~8–9 h | commits 5ab9096 (step-500 PASSED, eval 2.8567) + step-1000 milestone (eval 2.265 vs TU09FBO partial's 2.2755 — determinism check; ckpt-1000 2.54 GB on disk; eval ctx1024/batch2 no OOM); 2026-09-06 ~13:07 probe: step 1073, window 9.5-9.9 s/it → ETA ~10-11 h (thermal swing, normal); 2026-09-06 ~14:00 probe: step 1516, thermal swing ~13.2 s/it (normal), ETA ~20:00-21:30, completion watcher armed (background job; also flags a dead-training-process state); 2026-09-06 ~15:05 probe: step 2018, eval curve 2.8567@500 → 2.265@1000 → 2.0903@1500 → **1.9972@2000**; rotation VERIFIED (ckpt-500 dropped at save-2000; 1000/1500/2000 on disk; full 2.5 GB each); eval ctx1024/batch2 no OOM at every 500-step eval; **2026-09-06 16:22: user STOPPED the run at step 2292** — saved state = checkpoint-2000 (verified: global_step 2000, best 1.9972; ckpt-1000/1500/2000 on disk, ~2.5 GB each), ~292 steps since the last save lost (transformers saves on the 500-step cadence only — no save-on-interrupt); GPU freed, log frozen; RESUME = exact train command, zero flags, continues at 2001/5000; machine-move bundle BUILT 2026-09-06 ~16:45: `E:\python projects\nano_SLMs_m3_handoff\checkpoint-2000.zip` (2.72 GB, CRC-verified, sha256 7124b1…aa2d9e) + RESUME_ON_NEW_MACHINE.txt via scripts/bundle_checkpoint.py; `train_target.log`; `runs/target/logs/`; 2026-09-06 18:36: bundle ARRIVED on TU09FBO — resumed 2001/5000 (checkpoint-2000 extracted from the checkpoint_backup zip; best-path patched; cadence save+eval 100 / limit 3; transformers args-mismatch warning at resume is expected); 2026-09-06 19:07: nvlddmkm 153 GPU-driver fault killed the run pre-2100-save (not sleep/OOM/reboot); CUDA sanity PASS; RELAUNCHED ~19:11 from checkpoint-2000, verified at 2001/5000); 2026-09-07 07:14: user STOPPED at step 4593/5000 — last complete = checkpoint-4500 (11 files; eval 1.8610@4500, best 1.8512@4000), steps 4501–4593 discarded, zipped to checkpoint_backup\checkpoint-4500.zip (2,368 MB, 12 entries, tar exit 0); GPU freed (103 MiB); RESUME = exact train command → 4501/5000 |
| 2 | Disk-headroom watch during M3 | `in_progress` | 2026-09-06 probe: 10.13 GB free with ckpt-500+1000 both on disk (~5.1 GB); rotation (limit 3) caps steady state ~7.6 GB → comfortable; report before deleting anything; nothing without user approval. 2026-09-06 ~13:12 probe: 16.3 GB free (rose ~9 GB since the ~13:07 probe — not agent-initiated, likely user cleanup; re-measure at M3 completion) 2026-09-06 ~15:05 probe: **34.73 GB free** post-save-2000 (rose ~21 GB since the 13:12 probe — not trainer-initiated; all 3 checkpoints intact) 2026-09-06 18:36 probe: 32.2 GB free (run live on TU09FBO; 3 checkpoints + 2 zips on disk); 2026-09-07 07:14 post-stop probe: 33.3 GB free (dirs ckpt-3500/4000/4500 + checkpoint_backup zips 2000/4500) | free-disk probes this session; HANDOFF §3b |
| 3 | Post-M3 exit: `eval.py` on final + commit metrics + HANDOFF final row | `done` | #1 done. eval.py DONE 2026-09-07: runs/target/final/eval_report.json val_loss 1.8641, ppl 6.45; HANDOFF §2 + §3b final rows written; metrics commit `ba98cb1` (13 files: final-dir small files + train_summary + eval_report + tfevents + HANDOFF/TASKS; weights stay LOCAL per LFS decision — no LFS objects needed, tokenizer.json 3.5 MB plain git). Push pending user go | HANDOFF §8.2, §7; runs/target/final/eval_report.json |
| 4 | C12 Tier 1 — Evol-Instruct trace SFT of the M3 model | `done` | User decision 2026-09-06: run ONLY from runs/target/final (SFT-from-checkpoint-1000 declined — 20%-trained base; runbook decision log). **PILOT DONE 2026-09-07**: (1) c12_preflight --pilot PASS 8/8 GATE READY (GPU 7.5/8 free post-M3); (2) pilot sft.py --pilot PASSED — 5000 pairs, 1 epoch, 313 steps @ ~3 s/it (~17.5 min), train_loss 1.26, eval_loss 1.102→1.095, isolated in runs/sft_t1_pilot (final/ written, train_summary.json: base runs/target/final, params 226.5M); (3) eval.py on runs/sft_t1_pilot/final: forgetting guard CSN val_loss 1.9149 vs 1.8641 pre-SFT = +2.7% (gate ≤ +10-15% → PASS); instruction eval 50 held-out: greedy ast pass 0.60, sampled 0.78 (temp 0.8); eval_report.json in final/. **FULL RUN DONE 2026-09-07 (user go)**: full-mode preflight PASS 8/8 GATE READY (23.1 GB free, runs/sft_t1 clean) → sft.py --config configs/sft_t1.yaml: 16376 pairs x 2 epochs, 2048 steps @ ~2.6-3.3 s/it (~1h56m, zero interruptions), train_loss 0.868, eval_loss 0.836 (best = final @2048); runs/sft_t1/final written (model.safetensors 906 MB stays LOCAL per gitignore/LFS decision); eval.py on runs/sft_t1/final: instruction ast pass-rate greedy 0.86 / sampled 0.88 (pilot 0.60/0.78 — big gain); forgetting guard CSN val_loss 2.0995 vs 1.8641 pre-SFT = **+12.6% — within the ≤ +10-15% gate but near the upper edge** (honest note: capability-forgetting tradeoff favors instruction gain 0.60→0.86); eval_report.json in final/. Metrics commit `cd7b52d` PUSHED to origin/main (19 files: pilot+full small final-dir + tfevents + TASKS); prior `ba98cb1` (M3 metrics) also PUSHED. **Portability bundle BUILT 2026-09-07**: `checkpoint_backup/checkpoint-sft_t1-final.zip` (909.76 MB STORED zip64, 8 files at arcname `runs/sft_t1/final/...`, sha256 `2266c969…24a4f3`, verify OK) for inference + `checkpoint_backup/checkpoint-sft_t1-resume.zip` (2.72 GB STORED zip64, 11 files at arcname `checkpoint-2048/...`, sha256 `572cbba1…1f07e6`, verify OK — carries optimizer.pt 1.8 GB + scheduler.pt + scaler.pt + rng_state.pth for zero-flag SFT resume from step 2049). `checkpoint_backup/C12_T1_RESUME.txt` covers both zips: inference quickstart, SFT resume procedure (zero flags per PLAN §5.3 — identical to train.py), Tier 2/3 continuity (re-download teacher via scripts/download_teacher.py, ~3 min), full venv recipe, expected results, gotchas. NO-NEEDED-FILES CHECKLIST inside txt covers everything tracked-in-git vs needs-re-fetching. Next tier per plan: rows 5/7 (Tier 2/3, user-gated) | HANDOFF §8.4; research/c12_runbook.md; scripts/c12_preflight.py; research/c12_distillation_plan.md; checkpoint_backup/checkpoint-sft_t1-{final,resume}.zip |
| 5 | C12 Tier 2 (on-policy alignment) / Tier 3 (intra-ladder KD) | `pending` | User-gated (not yet approved). Teacher PICKED by user 2026-09-06: Qwen/Qwen3.5-0.8B - VERIFIED pre-download (ungated, ~1.8 GB, Apache-2.0, 24 text layers h1024, vocab 248320, ctx 262k, chat template; transformers 5.16.1 loads Qwen3_5ForConditionalGeneration natively; multimodal arch - judge uses the text tower only); DOWNLOADED to gitignored data/teacher/ (scripts/download_teacher.py, job exit 0, ~3 min - network far faster than the 230 KB/s estimate; local AutoConfig load OK). Exa context search UNBLOCKED 2026-09-06 (user added .env keys; payloads in research/raw/) | research/c12_tier2_brief.md; HF config probe 2026-09-06; research/qwen35_teacher_tasks.json |
| 6 | Upgrade path: pilot data quality (ungated raw code / the-stack-v2 with token); Flash-Next/GDN hybrid architecture | `pending` | User-gated; PLAN A7 = plain GQA first, hybrid is post-M3 only | HANDOFF §8.3; research/qwen3_8_flash_next_research.md |
| 7 | C12 Tier 2 readiness brief (milestone: user picked option 1 of the 2026-09-06 window menu) | `in_progress` | Design decision doc + VERIFIED bitsandbytes sm_75 SUCCESS + teacher costs + the tokenizer-mismatch finding (logit KL undefined cross-tokenizer -> V1 judge-rerank recommended). The Tier 2 RUN itself stays gated on #5/#1 + Tier 1 pass + explicit download/GPU approvals | research/c12_tier2_brief.md; c12_distillation_report.md §6 |
| 8 | scripts/status.py phase dashboard (milestone: user menu option 2) | `done` | Built + tested 2026-09-06: GPU/disk line + per-phase latest checkpoint, eval curves (trainer_state + TensorBoard), final summaries, eval reports incl. ast pass-rates; read-only, safe beside a live run; also surfaced a stray runs/pilot/checkpoint-1000/optimizer.pt (~0.8 GB, user-cleanup candidate) | scripts/status.py; README status line |
| 9 | LoRA/PEFT efficient fine-tune option (milestone: user menu option 3) | `done` | 2026-09-06 user OK'd install+design: peft 0.20.0 installed via uv (dry-run first: 12 new pkgs, ZERO upgrades - safe beside live M3), import verified (transformers 5.16.1 / torch 2.14.0+cu126), CPU LoRA smoke PASS (r=8: 3072/19648 trainable, exact math); design+cost doc (ladder = Llama arch, standard LoRA target names; T r=16 = 4,849,664 trainable = 2.14pct, est ~2.0-2.5 GB vs 4.24 measured full); config template | research/lora_peft_design.md; configs/lora_example.yaml |
| 10 | Implement the peft hook in train.py/sft.py (LoRA option) | `done` | **DONE 2026-09-07, all 4 acceptance criteria PASS.** (1) sanity_check PASS on configs/lora_example.yaml (peft block uncommented; trainable 2,605,056 = EXACT design number, 2.52%); (2) CPU e2e kill/resume drill PASS (checkpoint saves ADAPTER-ONLY (adapter_model.safetensors), resume continues, final = merged FULL model 8.3 MB via merge_and_unload in save_final); (3) vram_probe --lora MEASURED on GPU: 0.72 GB peak allocated vs 1.91 GB same-arch full FT = -62% VRAM at 2.5% trainable (1,693 tok/s vs 3,396 = -50% pace, expected for frozen base); (4) merged final loads in eval/infer unchanged (full safetensors + config). Gotchas fixed along the way: peft was missing on TU09FBO venv (installed, 1 pkg 0 upgrades); CUDA_VISIBLE_DEVICES empty-string does NOT hide GPU on Windows (MEMORY 12); kill-mid-checkpoint-write crash fixed via trainer_state.json completeness guard in find_latest_checkpoint (train.py+sft.py+kd.py, MEMORY 13) | research/lora_peft_design.md; src/model.py; MEMORY lessons 12-13 |
| 11 | Milestone B - Throughput: 8-bit Adam + batch/accum retune + env fingerprint | `done` | DONE 2026-09-07 (gates measured per protocol doc). Arms @500 steps, same data/seed: A fp32 eval **2.5644** / 2,801 tok/s / probe peak 1.91 GB; B 8-bit b1/a32 eval **2.5603** (delta -0.0041, parity gate +/-0.02 PASS) / 2,854 tok/s (1.02x pace gate PASS) / probe peak **1.36 GB** (-563 MiB >= 500 MiB VRAM gate PASS) / kill+resume drill PASS (auto-resumed ck251, no loss bump); C 8-bit b2/a16 eval 2.5604 (parity) but 1,680 tok/s = 0.60x pace FAIL -> REJECTED (batch 2 buys nothing on the 6 GB card). **DECISION: adamw_bnb_8bit + batch 1/accum 32 is the recorded default for the NEXT pretrain**; fp32 kept as fallback. LoRA hook (row 10) GPU-probed: 2.61M trainable, peak **0.72 GB**, 1,799 tok/s | research/milestone_b_8bit_ab.md; runs/pilot-{ab-base,b8,b8-r2x16}/final/train_summary.json; TASKS row 18 |
| 12 | Milestone C - Evaluation upgrade: execution-based mini-eval + tokens/sec | `done` | DONE 2026-09-06 (user GO window): scripts/mini_eval.py - 16 tasks x 2 tests, per-test subprocess+timeout (10 s, fresh temp workdir), canned self-test pass@1 1.0000 16/16 (harness mechanics proven), real CPU runs on runs/smoke/final x2 IDENTICAL (pass@1 0.0000 / test_credit 0.0000 both - greedy deterministic; 0.0 expected for a raw 200-step base model, harness measures post-SFT deltas); scripts/status.py tokens/sec + est-MFU lines live-tested read-only (target recent ~3,033 tok/s = ~10.8 s/it consistent with observed pace; MFU 20.4% fp32 / 4.4% fp16-tensor, est N=226.5M matches; pilot 2,021 tok/s full + MFU 17.2%/3.7% measured N); GPU attach + real SFT deltas post-M3 | scripts/mini_eval.py; scripts/status.py; runs/smoke/final/mini_eval_report.json |
| 13 | Milestone D - Data upgrade for the NEXT pretrain (row 6 successor): gated sources + mix | `in_progress` | User GO 2026-09-06 (window). DONE 2026-09-06: research/pretrain_mix_proposal.md (Exa-backed, 9 payloads in research/raw/milestone_d_*) - stack-v2 python impractical (233 GB dedup subset; Hub exposes blob-IDs only, needs AWS+SWH agreement), proposal = 30% the-stack-smol python + 55% starcoderdata python + 10% CSN python + 5% Evol-Instruct = ~134M train tokens at ctx 1024 (~230k rows), streaming-only download plan (~30-60 min realistic, 24.8 GB headroom left), 14-item acceptance checklist; prepare_data.py now auto-loads .env (gated unlock VERIFIED: the-stack-smol probe OK via HF_TOKEN). POST-M3: wire the 4-source chain (+ optional per-source target_rows) into the next-pretrain config, measure rows/tokens-per-row/dedupe per source, optional P-scale A/B | research/pretrain_mix_proposal.md; scripts/prepare_data.py; MEMORY data-source rows |
| 14 | Milestone E - Distillation path: tier-order decision + Tier 2 judge rubric | `done` | DONE 2026-09-06 (user GO window): decision framework written - recommendation Tier 3 BEFORE Tier 2 V1 (risk-ordered: same-tokenizer KL is clean + cheap, judge harness gets built in parallel, a KD'd S becomes the control that makes Tier 2's rerank delta measurable) with explicit reversal criteria; the decision itself executes at Tier 1 pass (user-owned); Tier 2 V1 judge contract FROZEN: strict-JSON verdict schema {verdict pass|partial|fail, score 0-2, reason}, 5-step parse ladder ending in ast.literal_eval fallback (never crashes a run; unparseable -> ast baseline), jsonl verdict logging + agreement rate + unparseable-rate gate (<10%), temp 0.2 / top_p 0.9 / max 120, teacher inference always STANDALONE | research/c12_tier_order_decision.md |
| 15 | Milestone F - Off-site backup of finals to a private HF Hub repo | `blocked` | BLOCKED on user: private HF Hub repo name (upload = external side effect needing approval). READY 2026-09-06: scripts/backup_to_hub.py - dry-run by default, token explicitly loaded from .env (repo pattern; hf_hub does not read .env), dry-run PASS (whoami ahmadsyy; runs/pilot/final manifest 7 files / 387.8 MiB; training_args.bin + optimizer.pt + checkpoint-* correctly excluded); real upload needs --repo <user>/<name> --execute and stays user-gated | scripts/backup_to_hub.py; MEMORY LFS row |
| 16 | Milestone G1 - S-scale hybrid-arch sandbox (GDN/Flash-Next validation at 12M) | `in_progress` | Design DONE 2026-09-06 (user GO window): research/gdn_sandbox_design.md (Exa-backed, payload-cited: chunked delta-rule recurrence, FLA fp32-internal cast as THE fp16 stability workaround, 3:1 GDN:gated-attn ratio x3 independent sources, attention sink 46.7->4.8%) - triton-free pure-PyTorch port path identified (Megatron-LM torch_chunk_gated_delta_rule deterministic mode over FLA kernels); S-scale validation protocol = sanity_check -> 200-step train -> eval vs param-matched arch:gqa control, 3 gates to earn a P-scale trial. IMPLEMENTATION POST-M3 ONLY (src/model.py untouched while M3 live - auto-resume contract) | research/gdn_sandbox_design.md; research/raw/milestone_g1_*.json; PLAN A7 |
| 17 | Milestone G2 - One-command custom pipeline (run_custom) | `done` | DONE 2026-09-06 (user GO window): scripts/run_custom.py - sanity_check -> prepare_data -> tokenize_data -> train -> eval with stop-on-fail, per-step timing + runs/<phase>/custom_chain.json report, --from resume, --dry-run; never-co-run guard: nvidia-smi compute-apps FILTERED to python processes (Windows WDDM quirk: every desktop app with a GPU context is listed - unfiltered it would abort every chain; filter verified live: shows ONLY the M3 trainer python PID, guard would correctly abort before train/eval); first real chain run post-M3 (custom_example) | scripts/run_custom.py; configs/custom_example.yaml |

| 18 | Milestone B GPU execution — 8-bit Adam A/B arms + LoRA hook + vram_probe (GPU window open post-M3/T1) | `done` | DONE 2026-09-07: arm A eval 2.5644 (2,801 tok/s, probe 1.91 GB); arm B 8-bit eval 2.5603 (2,854 tok/s, probe 1.36 GB, resume drill PASS ck251); arm C b2/a16 eval 2.5604 (1,680 tok/s, probe 1.56 GB, pace FAIL -> rejected). All gates measured; **8-bit Adam b1/a32 = default for next pretrain** (row 11); LoRA hook GPU-probed 0.72 GB peak / 2.61M trainable (row 10 GPU part DONE). GPU queue now hands to rows 19 (SFT v2 pilot) / 20 (KD baseline) | research/milestone_b_8bit_ab.md; runs/pilot-{ab-base,b8,b8-r2x16}/final/train_summary.json |
| 19 | SFT v2 — minimax3 distillation corpus (26,469 pairs / ~2.1M est tokens) from runs/target/final | `in_progress` | 2026-09-07 DATA PREP DONE: corpus inspected (combined/all_train.jsonl = 25,146 rows, keys instruction+response = direct sft_data.py match); GOTCHA: sft_t1's min_chars 200 dropped 81% of the corpus (20,448 rows — bugfix/completion shapes are legitimately short) -> min_chars 30 / min_instruction_chars 10 (corpus is teacher-validated + deduped upstream): kept 19,252 (18,752 train + 500 val; drop_short 5,777, drop_long 115 > ctx 512, drop_ast 0); configs/sft_v2.yaml written (base runs/target/final, lr 3e-5, epochs 2 -> ~2.3k steps, CSN forgetting guard kept). NEXT: pilot run -> eval -> full run when GPU frees | minimax3/data/distillation_data/; configs/sft_v2.yaml; data/sft/minimax3/ |
| 20 | C12 Tier 3 — intra-ladder KD, same-tokenizer (BEFORE Tier 2 per row 14 frozen decision) | `in_progress` | 2026-09-07 IMPLEMENTATION DONE + CPU-VALIDATED: scripts/kd.py (frozen teacher forward + KL(teacher||student, tau) + (1-alpha)*CE blend; eval stays PURE CE so load_best_model_at_end and the A/B stay valid; auto-resume identical incl. the partial-checkpoint guard); configs/kd_s_t1.yaml (teacher runs/pilot/final, student 4L/256 ~12.3M, pilot shards, 2000 steps batch 8) + configs/kd_s_baseline.yaml (fair from-scratch A/B, identical data/steps/seed); CPU rig PASS (tiny teacher, 4 steps, finite loss, kd summary fields). NEXT: baseline arm then KD arm on GPU (each ~1.5-3 h est), compare at 2000 and at 667 steps (1/3 read-out per plan §5) | research/c12_tier_order_decision.md; research/c12_distillation_plan.md §5; scripts/kd.py |
| 21 | Web UI (Gradio) — chat + data/config + train launch + monitor, per WEBUI_PRD.md | `in_progress` | **U1-U4 BUILT + VERIFIED 2026-09-07 (user go)**. U1 dashboard: all phases w/ ckpt/step/eval/report + detail, auto-refresh. U2 chat: picker lists ONLY weights-bearing dirs (target/sft_t1/sft_t1_pilot finals have NO weights on disk — they live in checkpoint_backup zips; load gracefully refuses), instruct template auto-default, VRAM policy verified on 2/3 branches (warn→CPU vs the LIVE Milestone-B run, real gpu_compute_pids hit, run untouched; reject→CPU simulated; GPU branch pending an idle-GPU window). U3 monitor: loss plot (matplotlib 3.11.1), progress bar + ETA (deduped TB steps — resume runs append duplicate step records), GPU line, log tail, 10 s refresh; HTTP 200. U4 train: name/preset/data/steps/lr → configs/webui_<name>.yaml (reserved names protected), preflight blocks on GPU-busy/low-disk/existing job, launch = run_custom.py chain, NO kill button, job_state.json; VERIFIED: config generated → sanity_check 4/4 PASS (12.3M), run_custom --dry-run clean, preflight blocks correctly w/ live GPU + passes w/ simulated idle, HTTP 200. HONEST NOTES: (1) sanity_check step briefly allocated 0.51 GB VRAM beside the live run during verification (momentary, no OOM; avoid re-running while training); (2) e2e chain launch + live-monitor refresh + U2 GPU branch all gated on an idle-GPU window (Milestone B arms still running); (3) psutil absent → _job_alive falls back to assume-not-live (compute-pids guard covers the real risk). NEXT: U5 polish items user-gated; live-run verifications on next free GPU | WEBUI_PRD.md; webui/app.py; README |

## Done recently (context)

- M0 smoke PASSED (kill/resume drill) · M1 VRAM probe PASSED (target approved,
  4.24/4.4 GB @ seq 512 and 1024) · M2 pilot PASSED (final ppl 3.19) — HANDOFF §2.
- C12 Tier 1 implemented + CPU-validated while M3 trains (2026-09-06).
- Post-M3 readiness audit + custom model/dataset support while M3 trains
  (2026-09-06): local-file sources in prepare_data.py (jsonl/json/txt/csv)
  and sft_data.py (+ generic instruction keys), sft.py --pilot output
  isolation, infer.py --sft instruct mode, configs/custom_example.yaml
  template (sanity_check 4/4 PASS, 100.7M as designed) — all CPU-tested;
  README documents both workflows.
- Window work 2026-09-06 afternoon: peft installed + LoRA design DONE
  (row 9; implementation = row 10, user-gated); Tier 2 teacher switched to
  Qwen/Qwen3.5-0.8B (verified + downloaded); exa-py installed, Exa helper
  mandated in AGENTS.md section 3 (EXA_API_KEY .env pending from user); 3 prep
  commits PUSHED to origin/main (user-approved).
- 2026-09-06 (same window): recommendation menu approved as MILESTONES ONLY
  (B/C/D/E/F/G -> rows 11-17) - created, not started; both API keys
  (.env EXA + HF) added by the user; teacher web-context rerun via Exa.
- M3 restarted fresh on MUO4QK5 (2026-09-06 10:56) — the TU09FBO handoff
  bundle never arrived (user call); prior partial run abandoned, its bundle/zip obsolete.
