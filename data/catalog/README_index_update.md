# xlsx note
xlsx COULD NOT be produced: openpyxl and xlsxwriter are absent from the venv and this repo is uv-managed/never-pip (AGENTS.md section 3). The clean CSVs below are canonical; they open in Excel directly (UTF-8, comma-separated, quoted fields).

Master (deduplicated, 88 rows): master_data_catalog.csv
Domain CSVs: audio_speech_datasets.csv (34) + audio_speech_datasets_2_raw.csv (31, complementary) + arm_datasets_emoji_title_schema.csv (28) + arabic_text_pashto_datasets_raw.csv (51, simplified header, full detail in the other files)
Negative findings: NEGATIVE_FINDINGS_AUDIO.md
