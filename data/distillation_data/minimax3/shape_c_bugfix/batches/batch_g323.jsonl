{"instruction": "Show silent except in `handler`. (input: '')", "response": "def handler():\n    try:\n        risky()\n    except Exception:\n        pass\n\n# Bug was: `pass` silently swallows all errors — at minimum log it\n\n# pair: handler / ''\n"}
