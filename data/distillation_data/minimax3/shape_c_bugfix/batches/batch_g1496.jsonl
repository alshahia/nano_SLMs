{"instruction": "Show `operator.itemgetter` in `op_itemgetter`. (input: [('a', 1), ('b', 2)])", "response": "import operator\n\ndef op_itemgetter(xs, i):\n    g = operator.itemgetter(i)\n    return [g(p) for p in xs]\n\n# Bug was: `itemgetter(i)` is faster than `lambda x: x[i]` for hot loops\n\n# pair: op_itemgetter / [('a', 1), ('b', 2)]\n"}
{"instruction": "Show `operator.itemgetter` in `op_itemgetter`. (input: 1)", "response": "import operator\n\ndef op_itemgetter(xs, i):\n    g = operator.itemgetter(i)\n    return [g(p) for p in xs]\n\n# Bug was: `itemgetter(i)` is faster than `lambda x: x[i]` for hot loops\n\n# pair: op_itemgetter / 1\n"}
