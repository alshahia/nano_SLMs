{"instruction": "Show bare `raise e` in `reraise_v1`. (input: '')", "response": "def reraise_v1():\n    try:\n        risky()\n    except Exception as e:\n        raise e\n\n# Bug was: `raise e` rebinds — original traceback lost; use bare `raise`\n\n# pair: reraise_v1 / ''\n"}
