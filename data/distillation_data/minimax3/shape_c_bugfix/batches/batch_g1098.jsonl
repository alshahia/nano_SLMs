{"instruction": "Show the `@contextmanager` decorator in `ctx_mgr_dec`. (input: 'hello')", "response": "from contextlib import contextmanager\n\n@contextmanager\ndef ctx_mgr_dec():\n    print('enter')\n    try:\n        yield\n    finally:\n        print('exit')\n\n# Bug was: `@contextmanager` turns a generator into a context manager; cleanest way to make custom ones\n\n# pair: ctx_mgr_dec / 'hello'\n"}
