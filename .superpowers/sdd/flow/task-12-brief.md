# Task 12 brief — requirements verbatim (part)



## Orchestrator additions (binding)
- G1 (already evidenced): scaffold+build+tests at cfa1b0d/ec891c7 — record PASS in ledger/files with commands.
- G2: manual validation script run as REAL HTTP checks (server running via & .\.venv\Scripts\python.exe -m flow.server.app --port 3010, in a managed background job): (a) valid linear chain graph PUT /api/flows + POST /api/validate ok:true; (b) shard-dir edge fan-in (dataset->train direct) rejected with clear error; (c) train without shard input -> missing-input error; (d) reserved-name 'smoke' rejected; (e) rows>500k rejected; (f) branching graph rejected (linear-chains-only). Evidence = actual HTTP request/response logs saved under .superpowers/sdd/flow/gate-evidence/.
- G3 (GPU run): only if TASKS.md/status shows NO live GPU job; if one is live, record SKIPPED with honest reason + plan note (must not OOM the run). Otherwise: POST /api/run on a smoke-phase graph -> Monitor-equivalent confirmation (status endpoint tail lines showing train progress; checkpoint-aligned stop via POST stop; rerun with zero flags to proven auto-resume contract). NOT a full run — a bounded window then stop; evidence: tail excerpt + trainer_state step delta.
- G4: configs/flow_<name>.yaml produced by the G2/G3 flow must match webui build_config-equivalent semantic keys (spot-check vs configs/smoke.yaml + webui PRESETS copy — via config_gen parity tests already in suite; record final diff).
- Browser drill (agent-browser skill pattern, prior ledger precedent: agent-browser daemon inside a harness background job): UI on 3010 prod static (build first), drill list: shell renders; 6 palette cards; add 2 nodes via palette drag AND right-click; wire dataset->prepare; invalid dataset->train connect refused with toast; save flow + open picker; Validate (OK toast); Preview card renders after ok; Run disabled while running NOT exercised without backend run: run the drill with the real server and a harmless graph -> a smoke REQUIRED per G3 anyway; Stop button appears while running; chat handoff dialog opens portal Render the Infer node button (manual instructions dialog).
- Docs to update (this agent): flow/README.md create (run steps: build, server --port, dev/prod, node contract summary, GPU lock/no-kill policy); AGENTS.md §2 layout + §3 commands row for flow; ENVIRONMENT.md append fastapi/uvicorn + pnpm approve-builds esbuild note; TASKS.md milestone rows; HANDOFF.md entry; MEMORY.md lesson (stale-flag/TOCTOU + api contract handoff pattern) if earned.
- Final whole-branch review: dispatch a fresh subagent with the entire feature diff (2acabda..HEAD), the T7 API contract, and the earlier-triage Minor list for go/no-go on merge to main (repo convention: stays on main per user choice A).
