# SDD progress ledger — Mounting (TASKS row 49)

Plan: docs/plans/2026-09-11_mounting_impl_plan.md (commit e16ef6f). Executed Subagent-Driven, controller-owned GPU gating.

BASE at dispatch: e16ef6f.
Global constraints: venv python only (& .\.venv\Scripts\python.exe); fp16 only (sm_75); train scripts must keep zero-flag auto-resume (path-pass + partial-ckpt guard); mount additive-only — scripts/train.py, sanity_check.py, src/model.py and all existing configs byte-identical; teacher weights never trained; GPU work strictly sequential + co-run safety (a live kt2_judge --stage sample run occupies the GPU as of session start — DO NOT launch GPU work before Task 5.4/6 without user OK).

Step 5.4: PASS (vram_probe mount-gate: peak 1.36 GB alloc / 1.52 GB reserved, bare student 3796 tok/s — well under 6 GB).
Step 5.5 drill: PASS after a 4-fix cycle — drill exposed 3 REAL bugs fixed by subagent 99220c40: e9feff5 collator (remove_unused_columns=False + label_names=['input_ids']) also un-deadened pure-CE eval; f97dae7 fp32 teacher dtype (bf16xF32 matmul crash); b77f82b bridges registered once in wrappers + 083b2d1 test contract line (dup shared-tensor save crash hit EVERY bridge-mode checkpoint save — fill drills drilled fill only, passed first run 3-step GPU: eval 10.542, clean 110-tensor final). Drill (gate, save/eval 50): kill after complete ckpt-50 -> exact-command zero-flag resume at step 51 -> eval 6.69@50/5.06@100/3.97@150 continuous, global_step 150 exit 0, cliff book persisted, final independence 110 tensors zero mount-named. Scratch (runs/mount_gate_drill runs/mount_fill_drill3 configs/*drill* drillcap*.txt ~4 GB) deletion recommended+pending; no cliffs exercised in drill (evals kept improving).
Task 5 (CPU part): complete (commit a5056a1, 5 configs, CPU-pinned sanity exit 0 x5; literal 4/4 gpu-line needs GPU — Step 5.3 official evidence is the '-1' re-run). Deviations: mount_drop carries inert mount.gate per I1 (code-verified inert in drop mode); control pins optim adamw_bnb_8bit explicitly (train.py default is adamw_torch). INCIDENT: implementer's first sanity pass TOUCHED GPU (~0.95 GB x5 transient, CUDA_VISIBLE_DEVICES='' gotcha) beside the live kt2 judge — later runs pinned '-1'; no apparent harm (judge survived, pids rotated 23428→23964). Task 5 (CPU part) REVIEWED: SPEC COMPLIANT / QUALITY PASS. GPU handoff notes: foreign judge pid 23964 resident (NEVER kill; wait/user), 5.4 expect <<6 GB, 5.5 drill + one unpinned sanity for the literal 4/4 record, Task 6 first-3-steps assertion for I2 (fill's feat_alpha default 0.5 unpinned — record), machine-bound absolute teacher_path noted for HANDOFF 3b, '' shows cuda (always '-1'). Minors: feat_alpha unpinned, absolute teacher_path, heads_bridge dead key. GPU state at 15:5x: kt2_judge --stage score live (pids 23964+3464), 5055/6144 MiB, 28% util; Steps 5.4/5.5 + Task 6 gated on user.
Task 4: complete (commit 54c030e; review PASS/minor-only; adjudication ACCEPTED — the brief's a=0/strength-1 assert is literally present under nonzero KV; mixing proof added via a.fill_(0.1)). Minors: numel-only detach compare, private-attr access, implicit grad truthiness.
Task 3: complete (dc7c20d F1 fix + 107cc13 trainer/dataset; review PASS/PASS, ran on the settled report). Task 5 bindings: I1 — needs the gate block in every mount config incl. fill (no fetch loss); I2 — Task 6 runs the first-3-steps GPU assertion covering fill hooks + checkpointing + fp16 (GPU only). Minors: heads_bridge key unused at hidden 768, first-eval can't cliff (by design), tiny crash window eval→mount_schedule write, no trailing newline, broad 'bridge' header substring. Never claim full severance (keep-one rule keeps it unreachable; drop never teacher-skips).
Task 2: complete (commit 79d2afe; reviewer PASS/APPROVED; 63/63 independent checks). Adjudications: WrappedLayer container-preserving return = NECESSARY deviation (5.16.1 LlamaDecoderLayer returns plain tensor). IMPORTANT F1 OPEN for user: brief's double zero-init (a=0 AND out_proj zero) makes bridge grads exactly 0 = permanently inert mount; fixes preserving step-0 identity: (a) random out_proj + a=0 [reviewer-verified trainable], (b) trainer perturbs a once at mount. F4: hidden_states[l] recorded PRE-bridge — Task 3 must pick anchor semantics. Minors: F2 report anchor-list typo, F3 unused import math, F5 pct-seed.
Task 1: complete (teacher stream committed; reviewer PASS/APPROVED; minor: meta.json single-run artifact — train counts from .bin sizes, NOT meta.json).
Task 0: complete (controller) — preflights: torch OK, pilot tokens OK, SmolLM2-135M cache ABSENT (download task assigned to Task 1 subagent), GPU BUSY (kt2_judge.py --stage sample, pid 23428, 712 MiB). Disk 22.04 GB free at start (was ~36; report any further drop).
