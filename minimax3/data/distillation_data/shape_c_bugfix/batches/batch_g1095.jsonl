{"instruction": "Show the bare `raise` re-raise pattern in `raise_bare`. (input: )", "response": "def raise_bare():\n    try:\n        risky()\n    except:\n        raise\n\n# Bug was: bare `raise` re-raises the current exception; useful in except blocks\n\n# pair: raise_bare /\n"}
