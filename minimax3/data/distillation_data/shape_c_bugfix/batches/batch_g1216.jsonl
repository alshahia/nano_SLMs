{"instruction": "Show `contextlib.redirect_stdout` in `ctx_redirect`. (input: 'hello')", "response": "import io\nfrom contextlib import redirect_stdout\n\ndef ctx_redirect():\n    buf = io.StringIO()\n    with redirect_stdout(buf):\n        print('hello')\n    return buf.getvalue()\n\n# Bug was: `redirect_stdout` captures print output; useful for testing\n\n# pair: ctx_redirect / 'hello'\n"}
