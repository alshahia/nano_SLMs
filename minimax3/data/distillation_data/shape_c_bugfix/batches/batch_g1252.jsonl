{"instruction": "Show the lazy formatting in logging in `log_lazy`. (input: 'hello')", "response": "import logging\n\nlogger = logging.getLogger(__name__)\n\ndef log_lazy(s):\n    logger.info('got %s', s)\n\n# Bug was: logger methods are lazy; format args only when the level is enabled\n\n# pair: log_lazy / 'hello'\n"}
