{"instruction": "Show `re.compile` in `re_compile`. (input: 'hello world')", "response": "import re\n\npat = re.compile('hello')\n\ndef re_compile(s, text):\n    return pat.search(text) is not None\n\n# Bug was: compiled patterns are reusable and faster for repeated use\n\n# pair: re_compile / 'hello world'\n"}
{"instruction": "Show `re.compile` in `re_compile`. (input: 'hello')", "response": "import re\n\npat = re.compile('hello')\n\ndef re_compile(s, text):\n    return pat.search(text) is not None\n\n# Bug was: compiled patterns are reusable and faster for repeated use\n\n# pair: re_compile / 'hello'\n"}
