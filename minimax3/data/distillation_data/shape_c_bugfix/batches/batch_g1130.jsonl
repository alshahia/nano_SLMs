{"instruction": "Show the `__contains__` method in `class_contains`. (input: [1, 2, 3])", "response": "class C:\n    def __init__(self, xs):\n        self.xs = xs\n    def __contains__(self, x):\n        return x in self.xs\n\n# Bug was: `__contains__` enables `in` operator; falls back to `__iter__` if missing\n\n# pair: class_contains / [1, 2, 3]\n"}
{"instruction": "Show the `__contains__` method in `class_contains`. (input: 2)", "response": "class C:\n    def __init__(self, xs):\n        self.xs = xs\n    def __contains__(self, x):\n        return x in self.xs\n\n# Bug was: `__contains__` enables `in` operator; falls back to `__iter__` if missing\n\n# pair: class_contains / 2\n"}
