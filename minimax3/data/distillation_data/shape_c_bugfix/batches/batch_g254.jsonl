{"instruction": "The function `bad_default_dict(key, cache={})` shares a cache across calls. Fix the default. (input: demo)", "response": "def bad_default_dict(key, cache={}):\n    cache[key] = True\n    return cache\n    # Bug was: default {} is shared across calls; use None and create inside.\n\n# pair: bad_default_dict / demo\n"}
