{"instruction": "Show banker's rounding in `round_banker`. (input: 2.5)", "response": "def round_banker(x):\n    return round(x)\n\n# Bug was: `round(2.5)` is 2 (banker's rounding); `round(1.5)` is 2 (also banker's). Both round to even\n\n# pair: round_banker / 2.5\n"}
