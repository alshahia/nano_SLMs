#!/usr/bin/env python3
"""Shape A topics for gen_topup3.

30 topics, ~8 names x ~8 examples each = ~64 pairs/topic.
Index range in shape_a_instruction_code/batches/: batch_g021.jsonl .. batch_g050.jsonl.
"""
A_TOPICS = [
    ("reverse_string",
     ["reverse_string", "str_reverse", "reverse_str", "string_reverse",
      "do_reverse", "make_reverse", "reversed_str", "flip_string"],
     ["'hello'", "'Python'", "'a'", "''", "'racecar'", "'12345'",
      "'Hello, World!'", "'tabs\there'"],
     ["def {FN}(s):",
      "    return s[::-1]"],
     "Write a function named `{FN}(s)` that returns the reversed string. Example: `{FN}({EX})`."),

    ("sort_dict_by_value",
     ["sort_dict_by_value", "dict_sort_value", "sort_by_value",
      "value_sorted_dict", "sort_dict_values", "rank_dict",
      "dict_by_value", "order_by_value"],
     ["{'a': 3, 'b': 1, 'c': 2}", "{'x': 10, 'y': -5}",
      "{'one': 1}", "{}", "{'a': 1, 'b': 1, 'c': 1}",
      "{'k': 'z', 'a': 'm', 'b': 'a'}",
      "{'p': 0.5, 'q': 0.1, 'r': 0.9}", "{'n': 100}"],
     ["def {FN}(d):",
      "    return dict(sorted(d.items(), key=lambda kv: kv[1]))"],
     "Write a function `{FN}(d)` returning the dict sorted by value (ascending). Example: `{FN}({EX})`."),

    ("sort_dict_by_key",
     ["sort_dict_by_key", "dict_sort_key", "sort_by_key",
      "key_sorted_dict", "sort_dict_keys", "alphabetical_dict",
      "dict_by_key", "order_by_key"],
     ["{'b': 1, 'a': 2, 'c': 3}", "{'z': 1, 'a': 2}",
      "{'B': 1, 'a': 2, 'C': 3}", "{}",
      "{'10': 1, '2': 2, '1': 3}",
      "{'k1': 'v', 'k2': 'w'}", "{'aa': 1, 'a': 2}"],
     ["def {FN}(d):",
      "    return dict(sorted(d.items()))"],
     "Write a function `{FN}(d)` returning the dict sorted by key (ascending). Example: `{FN}({EX})`."),

    ("count_occurrences",
     ["count_occurrences", "occurrences", "freq_of", "count_item",
      "how_many", "tally", "count_value", "element_count"],
     ["([1, 2, 2, 3, 2], 2)", "(['a', 'b', 'a'], 'a')",
      "([True, False, True], True)", "([1, 2, 3], 4)",
      "([], 1)", "(['x', 'y'], 'z')",
      "([1, 1, 1, 1, 1], 1)", "(['a'], 'a')"],
     ["def {FN}(xs, target):",
      "    return xs.count(target)"],
     "Write a function `{FN}(xs, target)` returning how many times `target` appears. Example: `{FN}({EX})`."),

    ("safe_divide",
     ["safe_divide", "div_safe", "try_divide", "divide_safe",
      "safe_div", "guarded_divide", "safe_quotient", "do_safe_div"],
     ["(10, 2)", "(10, 0)", "(0, 5)", "(-6, 2)",
      "(7, 3)", "(1, 1)", "(100, 25)", "(-1, -1)"],
     ["def {FN}(a, b):",
      "    if b == 0:",
      "        return None",
      "    return a / b"],
     "Write a function `{FN}(a, b)` that returns a/b, or None if b is 0. Example: `{FN}({EX})`."),

    ("mean",
     ["mean", "average", "avg", "compute_mean",
      "list_mean", "arithmetic_mean", "calc_avg", "do_mean"],
     ["[1, 2, 3, 4]", "[10]", "[]", "[-1, 1]",
      "[1.5, 2.5, 3.5]", "[0, 0, 0]",
      "[100, 200, 300]", "[1, 2]"],
     ["def {FN}(xs):",
      "    if not xs:",
      "        return 0",
      "    return sum(xs) / len(xs)"],
     "Write a function `{FN}(xs)` returning the arithmetic mean. Example: `{FN}({EX})`."),

    ("median",
     ["median", "middle_value", "find_median", "compute_median",
      "list_median", "do_median", "calc_median", "midpoint"],
     ["[1, 2, 3]", "[1, 2, 3, 4]", "[5]", "[]",
      "[10, 1, 5, 2]", "[3, 1, 4, 1, 5]",
      "[-1, -2, -3]", "[1.5, 2.5]"],
     ["def {FN}(xs):",
      "    if not xs:",
      "        return None",
      "    s = sorted(xs)",
      "    n = len(s)",
      "    return s[n // 2] if n % 2 else (s[n // 2 - 1] + s[n // 2]) / 2"],
     "Write a function `{FN}(xs)` returning the median. Example: `{FN}({EX})`."),

    ("variance",
     ["variance", "var", "compute_variance", "calc_var",
      "list_variance", "do_var", "sample_variance", "pop_variance"],
     ["[1, 2, 3, 4, 5]", "[10]", "[1, 1, 1]", "[-1, 0, 1]",
      "[2.5, 7.5]", "[0, 0, 0, 0]",
      "[100, 200]", "[1]"],
     ["def {FN}(xs):",
      "    if not xs:",
      "        return 0",
      "    m = sum(xs) / len(xs)",
      "    return sum((x - m) ** 2 for x in xs) / len(xs)"],
     "Write a function `{FN}(xs)` returning the population variance. Example: `{FN}({EX})`."),

    ("is_anagram",
     ["is_anagram", "anagram_check", "check_anagram", "anagrams",
      "are_anagrams", "verify_anagram", "anagram_test", "is_anagram_of"],
     ["('listen', 'silent')", "('hello', 'world')",
      "('', '')", "('a', 'a')",
      "('Triangle', 'integral')", "('abc', 'abcd')",
      "('Listen', 'Silent')", "('aa', 'bb')"],
     ["def {FN}(a, b):",
      "    return sorted(a.lower()) == sorted(b.lower())"],
     "Write a function `{FN}(a, b)` that returns True iff `a` and `b` are anagrams. Example: `{FN}({EX})`."),

    ("is_subsequence",
     ["is_subsequence", "subsequence_of", "is_subseq",
      "check_subsequence", "subseq_check", "in_order",
      "is_substring_of", "contains_sequence"],
     ["('abc', 'axbycz')", "('abc', 'acb')",
      "('', 'anything')", "('a', 'a')",
      "('ace', 'abcde')", "('xyz', 'abc')",
      "('ab', 'abab')", "('hello', 'world hello')"],
     ["def {FN}(sub, s):",
      "    it = iter(s)",
      "    return all(c in it for c in sub)"],
     "Write a function `{FN}(sub, s)` that returns True iff `sub` is a subsequence of `s`. Example: `{FN}({EX})`."),

    ("longest_common_prefix",
     ["longest_common_prefix", "lcp", "common_prefix",
      "shared_prefix", "find_prefix", "prefix_of_all",
      "common_start", "do_lcp"],
     ["(['flower', 'flow', 'flight'])",
      "(['dog', 'racecar', 'car'])",
      "(['interspecies', 'interstellar', 'interstate'])",
      "(['throne'])", "([])",
      "(['a'])", "(['abc', 'abc', 'abc'])",
      "(['abc', 'abd', 'abe'])"],
     ["def {FN}(strs):",
      "    if not strs:",
      "        return ''",
      "    p = strs[0]",
      "    for s in strs[1:]:",
      "        while not s.startswith(p):",
      "            p = p[:-1]",
      "            if not p: return ''",
      "    return p"],
     "Write a function `{FN}(strs)` returning the longest common prefix. Example: `{FN}({EX})`."),

    ("capitalize_each_word",
     ["capitalize_each_word", "title_case", "capitalize_words",
      "titlecase", "word_title", "capitalize_all",
      "do_cap_words", "words_capitalize"],
     ["'hello world'", "'the quick brown fox'",
      "''", "'already Title Case'",
      "'one'", "'a b c d e'",
      "'PYTHON'", "'mixed CASE here'"],
     ["def {FN}(s):",
      "    return ' '.join(w.capitalize() for w in s.split())"],
     "Write a function `{FN}(s)` capitalizing the first letter of each word. Example: `{FN}({EX})`."),

    ("word_frequency",
     ["word_frequency", "word_freq", "freq_words",
      "term_frequency", "word_counts", "count_words_dict",
      "do_word_freq", "tf_simple"],
     ["'the cat sat on the mat'",
      "'hello hello world'",
      "''", "'one'",
      "'a b a c a b'", "'The the THE'",
      "'line1\nline2 line1'", "'tabs\there\there'"],
     ["def {FN}(s):",
      "    out = {}",
      "    for w in s.lower().split():",
      "        out[w] = out.get(w, 0) + 1",
      "    return out"],
     "Write a function `{FN}(s)` returning a word -> count dict. Example: `{FN}({EX})`."),

    ("is_valid_email_simple",
     ["is_valid_email_simple", "valid_email",
      "check_email", "is_email", "email_ok",
      "validate_email", "email_valid", "is_email_addr"],
     ["'alice@example.com'", "'bob@foo.co.uk'",
      "'not-an-email'", "'@nodomain.com'",
      "'noat.com'", "'a@b.c'",
      "'user.name+tag@sub.domain.org'", "''"],
     ["import re",
      "def {FN}(s):",
      "    return bool(re.match(r'^[\\w.+-]+@[\\w-]+\\.[\\w.-]+$', s))"],
     "Write a function `{FN}(s)` returning True iff `s` looks like an email. Example: `{FN}({EX})`."),

    ("celsius_to_fahrenheit",
     ["celsius_to_fahrenheit", "c_to_f", "c2f",
      "celsius_to_f", "convert_c_to_f", "do_c_to_f",
      "fahrenheit_of", "ctof"],
     ["0", "100", "-40", "37",
      "25", "-273.15", "50", "212"],
     ["def {FN}(c):",
      "    return c * 9 / 5 + 32"],
     "Write a function `{FN}(c)` converting Celsius to Fahrenheit. Example: `{FN}({EX})`."),

    ("fahrenheit_to_celsius",
     ["fahrenheit_to_celsius", "f_to_c", "f2c",
      "fahrenheit_to_c", "convert_f_to_c", "do_f_to_c",
      "celsius_of", "ftoc"],
     ["32", "212", "-40", "98.6",
      "0", "-459.67", "50", "100"],
     ["def {FN}(f):",
      "    return (f - 32) * 5 / 9"],
     "Write a function `{FN}(f)` converting Fahrenheit to Celsius. Example: `{FN}({EX})`."),

    ("kg_to_lb",
     ["kg_to_lb", "kilograms_to_pounds", "kgs_to_lbs",
      "kg2lb", "convert_kg_to_lb", "do_kg_to_lb",
      "pounds_of", "kilograms_in_pounds"],
     ["0", "1", "50", "100",
      "-1", "0.5", "75.5", "1000"],
     ["def {FN}(kg):",
      "    return kg * 2.20462"],
     "Write a function `{FN}(kg)` converting kilograms to pounds. Example: `{FN}({EX})`."),

    ("roman_to_int",
     ["roman_to_int", "parse_roman", "roman_parse",
      "roman_value", "from_roman", "decode_roman",
      "roman_to_num", "r2i"],
     ["'III'", "'IV'", "'IX'",
      "'LVIII'", "'MCMXCIV'",
      "''", "'I'", "'MMXXIV'"],
     ["def {FN}(s):",
      "    vals = {'I': 1, 'V': 5, 'X': 10, 'L': 50,",
      "            'C': 100, 'D': 500, 'M': 1000}",
      "    out = 0",
      "    for i, c in enumerate(s):",
      "        if i + 1 < len(s) and vals[c] < vals[s[i+1]]:",
      "            out -= vals[c]",
      "        else:",
      "            out += vals[c]",
      "    return out"],
     "Write a function `{FN}(s)` parsing a Roman numeral into an integer. Example: `{FN}({EX})`."),

    ("int_to_roman",
     ["int_to_roman", "to_roman", "make_roman",
      "roman_of", "num_to_roman", "i2r",
      "format_roman", "roman_string"],
     ["3", "4", "9", "58",
      "1994", "1", "0", "3999"],
     ["def {FN}(n):",
      "    pairs = [(1000,'M'),(900,'CM'),(500,'D'),(400,'CD'),",
      "             (100,'C'),(90,'XC'),(50,'L'),(40,'XL'),",
      "             (10,'X'),(9,'IX'),(5,'V'),(4,'IV'),(1,'I')]",
      "    out = ''",
      "    for v, s in pairs:",
      "        while n >= v:",
      "            out += s",
      "            n -= v",
      "    return out"],
     "Write a function `{FN}(n)` returning the Roman numeral for 1..3999. Example: `{FN}({EX})`."),

    ("dec_to_bin",
     ["dec_to_bin", "decimal_to_binary", "int_to_bin",
      "to_binary", "bin_string", "do_dec_to_bin",
      "binary_of", "dec2bin"],
     ["0", "1", "5", "10",
      "255", "-1", "1024", "42"],
     ["def {FN}(n):",
      "    if n == 0:",
      "        return '0'",
      "    return bin(n)[2:] if n > 0 else '-' + bin(-n)[2:]"],
     "Write a function `{FN}(n)` returning the binary string for `n`. Example: `{FN}({EX})`."),

    ("bin_to_dec",
     ["bin_to_dec", "binary_to_decimal", "bin_to_int",
      "from_binary", "parse_bin", "do_bin_to_dec",
      "decimal_of_bin", "bin2dec"],
     ["'0'", "'1'", "'101'", "'1010'",
      "'11111111'", "'-101'", "'10000000000'", "'42'"],
     ["def {FN}(s):",
      "    return int(s, 2)"],
     "Write a function `{FN}(s)` parsing a binary string into an int. Example: `{FN}({EX})`."),

    ("is_leap_year",
     ["is_leap_year", "leap_year", "check_leap",
      "is_leap", "leap_check", "year_is_leap",
      "verify_leap", "leap_of"],
     ["2000", "1900", "2024", "2023",
      "2100", "2400", "1", "4"],
     ["def {FN}(y):",
      "    return y % 400 == 0 or (y % 4 == 0 and y % 100 != 0)"],
     "Write a function `{FN}(y)` returning True iff `y` is a leap year. Example: `{FN}({EX})`."),

    ("days_in_month",
     ["days_in_month", "month_days", "days_of_month",
      "month_length", "days_in", "how_many_days",
      "days_per_month", "monthday"],
     ["(1, 2024)", "(2, 2023)", "(2, 2024)",
      "(4, 2024)", "(12, 2024)",
      "(1, 2000)", "(2, 1900)", "(13, 2024)"],
     ["def {FN}(m, y):",
      "    if m == 2:",
      "        return 29 if y % 400 == 0 or (y % 4 == 0 and y % 100 != 0) else 28",
      "    return 30 if m in (4, 6, 9, 11) else 31"],
     "Write a function `{FN}(m, y)` returning days in month `m` of year `y`. Example: `{FN}({EX})`."),

    ("clamp_number",
     ["clamp_number", "clamp", "clip_value",
      "constrain", "clamp_val", "limit_range",
      "clamp_to_range", "do_clamp"],
     ["(5, 0, 10)", "(15, 0, 10)",
      "(-5, 0, 10)", "(0, 0, 10)",
      "(10, 0, 10)", "(0.5, 0, 1)",
      "(-1, -10, 10)", "(100, 0, 50)"],
     ["def {FN}(x, lo, hi):",
      "    return max(lo, min(hi, x))"],
     "Write a function `{FN}(x, lo, hi)` clamping x to [lo, hi]. Example: `{FN}({EX})`."),

    ("lerp",
     ["lerp", "linear_interp", "interpolate",
      "lin_interp", "linterp", "linear_interpolate",
      "do_lerp", "mix_value"],
     ["(0, 10, 0)", "(0, 10, 1)",
      "(0, 10, 0.5)", "(0, 10, 0.25)",
      "(-5, 5, 0.5)", "(0, 100, 0.1)",
      "(0, 1, 0)", "(1, 2, 2)"],
     ["def {FN}(a, b, t):",
      "    return a + (b - a) * t"],
     "Write a function `{FN}(a, b, t)` linear-interpolating between a and b at t. Example: `{FN}({EX})`."),

    ("unique_preserve_order",
     ["unique_preserve_order", "uniq_ordered", "ordered_unique",
      "uniq_keep_order", "stable_dedupe", "preserve_unique",
      "distinct_ordered", "do_uniq"],
     ["[1, 2, 2, 3, 1]", "['a', 'b', 'a', 'c']",
      "[]", "[1, 1, 1]", "[True, False, True]",
      "['x']", "[1, 2, 3, 4]", "[1, 1, 2, 2, 3, 3]"],
     ["def {FN}(xs):",
      "    seen = set()",
      "    out = []",
      "    for x in xs:",
      "        if x not in seen:",
      "            seen.add(x)",
      "            out.append(x)",
      "    return out"],
     "Write a function `{FN}(xs)` returning xs with duplicates removed, order preserved. Example: `{FN}({EX})`."),

    ("running_sum",
     ["running_sum", "cumulative_sum", "cumsum",
      "running_total", "prefix_sum", "scan_sum",
      "acc_sum", "do_running_sum"],
     ["[1, 2, 3, 4]", "[5]", "[]",
      "[-1, 1, -1, 1]", "[10, 20, 30]",
      "[0, 0, 0]", "[1.5, 2.5]", "[100]"],
     ["def {FN}(xs):",
      "    out = []",
      "    s = 0",
      "    for x in xs:",
      "        s += x",
      "        out.append(s)",
      "    return out"],
     "Write a function `{FN}(xs)` returning the running (cumulative) sum. Example: `{FN}({EX})`."),

    ("running_max",
     ["running_max", "cumulative_max", "cummax",
      "running_high", "prefix_max", "scan_max",
      "acc_max", "do_running_max"],
     ["[1, 3, 2, 4]", "[5]", "[]",
      "[-1, -2, -3]", "[10, 5, 8, 12]",
      "[0, 0, 0]", "[1.5, 2.5]", "[100]"],
     ["def {FN}(xs):",
      "    out = []",
      "    m = None",
      "    for x in xs:",
      "        m = x if m is None else max(m, x)",
      "        out.append(m)",
      "    return out"],
     "Write a function `{FN}(xs)` returning the running (cumulative) max. Example: `{FN}({EX})`."),

    ("any_match",
     ["any_match", "any_satisfy", "exists",
      "any_pred", "has_match", "matches_any",
      "any_of", "do_any_match"],
     ["([1, 2, 3], lambda x: x > 2)",
      "([1, 2, 3], lambda x: x > 5)",
      "([], lambda x: True)",
      "([0, 0, 1], lambda x: x)",
      "(['a', 'b'], lambda s: s == 'c')",
      "([1], lambda x: x == 1)",
      "([False, False], lambda x: x)",
      "([None, 1], lambda x: x is not None)"],
     ["def {FN}(xs, pred):",
      "    return any(pred(x) for x in xs)"],
     "Write a function `{FN}(xs, pred)` returning True iff pred(x) is true for any x. Example: `{FN}({EX})`."),

    ("all_match",
     ["all_match", "all_satisfy", "for_all",
      "all_pred", "matches_all", "every_match",
      "all_of", "do_all_match"],
     ["([1, 2, 3], lambda x: x > 0)",
      "([1, 2, 3], lambda x: x > 2)",
      "([], lambda x: False)",
      "([0, 0, 0], lambda x: x >= 0)",
      "(['a', 'b'], lambda s: len(s) == 1)",
      "([1], lambda x: x == 1)",
      "([True, True], lambda x: x)",
      "([1, 2, None], lambda x: x is not None)"],
     ["def {FN}(xs, pred):",
      "    return all(pred(x) for x in xs)"],
     "Write a function `{FN}(xs, pred)` returning True iff pred(x) is true for all x. Example: `{FN}({EX})`."),

    ("normalize_path",
     ["normalize_path", "path_clean", "resolve_dots"],
     ["'/usr/./local/../bin'", "'/a/b/./c/../d'", "'/./'", "'/x/y/'", "'/../'"],
     ["import os",
      "def {FN}(p):",
      "    return os.path.normpath(p)"],
     "Write a function `{FN}(p)` returning the normalized absolute path. Example: `{FN}({EX})`."),

    ("file_exists",
     ["file_exists", "path_exists", "check_file"],
     ["'/etc/hosts'", "'/nonexistent.txt'", "'.'", "''", "'/tmp'"],
     ["import os",
      "def {FN}(p):",
      "    return os.path.isfile(p)"],
     "Write a function `{FN}(p)` returning True iff the file exists. Example: `{FN}({EX})`."),

    ("dir_exists",
     ["dir_exists", "is_directory", "check_dir"],
     ["'/tmp'", "'/etc'", "'/nonexistent'", "'.'", "''"],
     ["import os",
      "def {FN}(p):",
      "    return os.path.isdir(p)"],
     "Write a function `{FN}(p)` returning True iff the directory exists. Example: `{FN}({EX})`."),

    ("list_dir",
     ["list_dir", "ls", "list_directory"],
     ["'/tmp'", "'.'", '/etc'", "'/usr'", "''],
     ["import os",
      "def {FN}(p):",
      "    return sorted(os.listdir(p)) if os.path.isdir(p) else []"],
     "Write a function `{FN}(p)` listing files in the directory. Example: `{FN}({EX})`."),

    ("with_lines",
     ["with_lines", "read_all_lines", "all_lines_of"],
     ["'/etc/hosts'", "'/dev/null'", "'/nonexistent'", "''", "'/'"],
     ["def {FN}(path):",
      "    try:",
      "        with open(path, 'r', encoding='utf-8') as f:",
      "            return f.read().splitlines()",
      "    except Exception:",
      "        return []"],
     "Write a function `{FN}(path)` returning all lines in the file. Example: `{FN}({EX})`."),

    ("json_load_file",
     ["json_load_file", "read_json", "load_json_path"],
     ["'config.json'", "'/etc/hosts'", "'nonexistent.json'", "''", "'/'"],
     ["import json",
      "def {FN}(path):",
      "    try:",
      "        with open(path, 'r', encoding='utf-8') as f:",
      "            return json.load(f)",
      "    except Exception:",
      "        return None"],
     "Write a function `{FN}(path)` loading JSON from a file. Example: `{FN}({EX})`."),

    ("json_dump_file",
     ["json_dump_file", "write_json", "save_json_to"],
     ["({'a': 1}, 'out.json')", "([1,2,3], 'data.json')", "({}, 'empty.json')",
      "({'k': 'v'}, 'kv.json')", "([], 'list.json')"],
     ["import json",
      "def {FN}(obj, path):",
      "    with open(path, 'w', encoding='utf-8') as f:",
      "        json.dump(obj, f, ensure_ascii=False, indent=2)"],
     "Write a function `{FN}(obj, path)` writing JSON to a file. Example: `{FN}({EX})`."),

    ("merge_intervals_basic",
     ["merge_intervals_basic", "merge_overlapping", "coalesce_intervals"],
     ["[[1,3],[2,6],[8,10],[15,18]]", "[[1,4],[4,5]]", "[[1,4]]",
      "[[1,5],[2,3]]", "[[1,2],[3,4],[5,6]]"],
     ["def {FN}(intervals):",
      "    intervals.sort()",
      "    out = []",
      "    for s, e in intervals:",
      "        if out and s <= out[-1][1]:",
      "            out[-1][1] = max(out[-1][1], e)",
      "        else:",
      "            out.append([s, e])",
      "    return out"],
     "Write a function `{FN}(intervals)` merging overlapping intervals. Example: `{FN}({EX})`."),

    ("is_sorted_asc",
     ["is_sorted_asc", "is_ascending", "in_order_check"],
     ["[1,2,3,4]", "[1,3,2]", "[1]", "[]", "[5,5,5]"],
     ["def {FN}(xs):",
      "    return all(xs[i] <= xs[i+1] for i in range(len(xs) - 1))"],
     "Write a function `{FN}(xs)` checking if list is sorted ascending. Example: `{FN}({EX})`."),

    ("is_sorted_desc",
     ["is_sorted_desc", "is_descending", "reverse_order_check"],
     ["[4,3,2,1]", "[1,3,2]", "[5]", "[]", "[5,5,5]"],
     ["def {FN}(xs):",
      "    return all(xs[i] >= xs[i+1] for i in range(len(xs) - 1))"],
     "Write a function `{FN}(xs)` checking if list is sorted descending. Example: `{FN}({EX})`."),

    ("count_occurrences_v2",
     ["count_occurrences_v2", "tally_v2", "freq_v2"],
     ["('hello world', 'o')", "('banana', 'a')", "('test', 'z')",
      "('aaa', 'a')", "('hello', 'l')"],
     ["def {FN}(s, c):",
      "    return s.count(c)"],
     "Write a function `{FN}(s, c)` counting occurrences of c in s. Example: `{FN}({EX})`."),

    ("detect_palindrome_v2",
     ["detect_palindrome_v2", "is_palindrome_v2", "palindrome_test_v2"],
     ["'racecar'", "'hello'", "''", "'a'", "'Madam'"],
     ["def {FN}(s):",
      "    cleaned = ''.join(c.lower() for c in s if c.isalnum())",
      "    return cleaned == cleaned[::-1]"],
     "Write a function `{FN}(s)` returning True iff s is a palindrome. Example: `{FN}({EX})`."),

    ("longest_word_in",
     ["longest_word_in", "longest_word", "max_word_len"],
     ["'hello world'", "'the quick brown fox'", "''", "'one'", "'a b c d e f g'"],
     ["def {FN}(s):",
      "    words = s.split()",
      "    return max(words, key=len) if words else ''"],
     "Write a function `{FN}(s)` returning the longest word in s. Example: `{FN}({EX})`."),

    ("shortest_word_in",
     ["shortest_word_in", "shortest_word", "min_word_len"],
     ["'hello world'", "'the quick brown fox'", "''", "'one'", "'a b c d e f g'"],
     ["def {FN}(s):",
      "    words = s.split()",
      "    return min(words, key=len) if words else ''"],
     "Write a function `{FN}(s)` returning the shortest word in s. Example: `{FN}({EX})`."),

    ("normalize_text",
     ["normalize_text", "clean_text_v2", "strip_diacritics_v2"],
     ["'café'", "'hello WORLD!'", "'no change'", "''", "'naïve'"],
     ["import unicodedata",
      "def {FN}(s):",
      "    nfkd = unicodedata.normalize('NFKD', s)",
      "    return ''.join(c for c in nfkd if not unicodedata.combining(c))"],
     "Write a function `{FN}(s)` normalizing unicode (removing diacritics). Example: `{FN}({EX})`."),

    ("chunk_by_predicate",
     ["chunk_by_predicate", "split_where", "group_by_boundary"],
     ["([1, 1, 2, 2, 3], lambda x: x == 2)",
      "([1, 2, 3, 1, 2], lambda x: x == 1)",
      "([1, 2, 3], lambda x: False)",
      "([], lambda x: True)",
      "([1, 1, 1], lambda x: x == 1)"],
     ["def {FN}(xs, pred):",
      "    chunks = []",
      "    cur = []",
      "    for x in xs:",
      "        if pred(x) and cur:",
      "            chunks.append(cur)",
      "            cur = []",
      "        cur.append(x)",
      "    if cur: chunks.append(cur)",
      "    return chunks"],
     "Write a function `{FN}(xs, pred)` chunking xs at pred boundaries. Example: `{FN}({EX})`."),

    ("sum_by_key",
     ["sum_by_key", "group_sum", "agg_sum"],
     ["[{'k': 'a', 'v': 1}, {'k': 'b', 'v': 2}, {'k': 'a', 'v': 3}], 'k'",
      "[{'x': 1, 'n': 5}, {'x': 2, 'n': 3}, {'x': 1, 'n': 2}], 'x'",
      "[], 'k'",
      "[{'a': 1, 'v': 10}], 'a'",
      "[{'type': 'x', 'count': 1}, {'type': 'y', 'count': 2}], 'type'"],
     ["from collections import defaultdict",
      "def {FN}(items, key):",
      "    out = defaultdict(int)",
      "    for it in items: out[it[key]] += it.get('v', it.get('n', it.get('count', 1)))",
      "    return dict(out)"],
     "Write a function `{FN}(items, key)` summing items by key. Example: `{FN}({EX})`."),

    ("first_or_default",
     ["first_or_default", "find_first_or", "head_or"],
     ["([1,2,3], lambda x: x > 1, 0)",
      "([1,2,3], lambda x: x > 5, -1)",
      "([], lambda x: True, None)",
      "([1,2,3], lambda x: x > 0, 0)",
      "([1], lambda x: x == 1, 99)"],
     ["def {FN}(xs, pred, default):",
      "    return next((x for x in xs if pred(x)), default)"],
     "Write a function `{FN}(xs, pred, default)` returning the first matching item or default. Example: `{FN}({EX})`."),

    ("drop_while",
     ["drop_while", "skip_until", "advance_until"],
     ["([1, 2, 3, 4], lambda x: x < 3)",
      "([1, 2, 3], lambda x: x < 0)",
      "([1, 2, 3], lambda x: x > 100)",
      "([], lambda x: True)",
      "([5, 1, 2], lambda x: x < 5)"],
     ["from itertools import dropwhile",
      "def {FN}(xs, pred):",
      "    return list(dropwhile(pred, xs))"],
     "Write a function `{FN}(xs, pred)` dropping items while pred is true. Example: `{FN}({EX})`."),

    ("take_while",
     ["take_while", "take_until", "collect_until"],
     ["([1, 2, 3, 4], lambda x: x < 3)",
      "([1, 2, 3], lambda x: x < 100)",
      "([1, 2, 3], lambda x: x < 0)",
      "([], lambda x: True)",
      "([1, 2, 1, 2], lambda x: x < 2)"],
     ["from itertools import takewhile",
      "def {FN}(xs, pred):",
      "    return list(takewhile(pred, xs))"],
     "Write a function `{FN}(xs, pred)` taking items while pred is true. Example: `{FN}({EX})`."),

    ("stack_push_pop",
     ["stack_push_pop", "stack_sim", "lifo_ops"],
     ["push_1", "push_2", "push_3", "pop_a", "pop_b", "pop_c"],
     ["def {FN}():",
      "    s = []",
      "    ops = []",
      "    cmds = ['push', 1], ['push', 2], ['push', 3], 'pop', 'pop', 'pop'",
      "    for cmd in cmds:",
      "        if cmd == 'push':",
      "            ops.append(('push',))",
      "        else:",
      "            ops.append(('pop',))",
      "    return ops"],
     "Write a function `{FN}()` returning the sequence of stack push/pop operations. Example: `{FN}({EX})`."),

    ("queue_enqueue_dequeue",
     ["queue_enqueue_dequeue", "queue_sim", "fifo_ops"],
     ["enq_a", "enq_b", "enq_c", "deq_a", "deq_b", "deq_c"],
     ["from collections import deque",
      "def {FN}():",
      "    q = deque()",
      "    ops = []",
      "    cmds = [['enq', 1], ['enq', 2], ['enq', 3], 'deq', 'deq', 'deq']",
      "    for cmd in cmds:",
      "        if cmd == 'enq':",
      "            q.append(1)",
      "            ops.append(('enq', 1))",
      "        else:",
      "            v = q.popleft() if q else None",
      "            ops.append(('deq', v))",
      "    return ops"],
      "Write a function `{FN}()` returning the sequence of queue enqueue/dequeue operations. Example: `{FN}({EX})`."),

    ("vowel_ratio",
     ["vowel_ratio", "vowel_pct", "vowel_density"],
     ["'hello'", "'xyz'", "''", "'aeiou'", "'Python'"],
     ["def {FN}(s):",
      "    if not s: return 0.0",
      "    v = sum(1 for c in s.lower() if c in 'aeiou')",
      "    return v / len(s)"],
     "Write a function `{FN}(s)` returning the ratio of vowels in s. Example: `{FN}({EX})`."),
]


